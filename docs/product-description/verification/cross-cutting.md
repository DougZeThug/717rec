# Verification: cross-cutting

How to run this file: these five documents describe behaviour that is not on any
one page, so the items move around the app rather than sitting on a route. Set
up more than usual before you start. You need a **desktop browser** with the
network panel and the element inspector open; a **phone**, or a browser at
390 x 844 with touch emulation and the ability to resize past 768 pixels; a
**private window** for the signed-out items; a **player** account with an
approved membership; an **admin** account; a **second browser** for the
two-people items; a way to go **offline** and to throttle to **slow**; and a
real **screen reader** for the accessibility items — the inspector's
accessibility tree answers some of them but never answers "was it announced".

Two limits are worth knowing before you record anything. **The dev server is a
development build**, and several claims here are about what a *published* build
does — analytics, error reporting, and session replay are all switched off
locally. Those items say so, and on a dev server the honest answer is `blocked`.
And the dev server writes to the league's **real database**: the handful of
items here that write anything are marked **(destructive)** and their Steps stop
short of the write wherever the claim can be checked without it.

These documents deliberately drop template sections — the five named phases,
and in three cases the Modifiers table — because a failure, a viewport, an
assistive technology and a recorded pageview are not forms and have no editing
phase. The items below cover what each document does contain; what a dropped
section would have covered is listed under "Not checkable by hand" where it
still matters. Device values: `browser`, `phone`, `visitor`, `player`, `admin`,
`second scorer`, `offline`, `slow`, `second browser`.

## cross-cutting/permissions.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PERM-01 | P1 | player | The "Access Denied" toast is the only place in the product where a refusal is stated in words ([The simple case](../cross-cutting/permissions.md#the-simple-case)). | Signed in, not an admin. | 1. Type `/admin` and read the toast word for word.<br>2. Note where you land.<br>3. Across the rest of the app, find any other message that names permission as the reason for a refusal. | Exactly one red toast reading "Access Denied — You do not have admin privileges", then the home page. No other refusal anywhere in the app is stated in words. | — |
| PERM-02 | P1 | visitor | The message board renders for a visitor and shows "No Messages Yet — Be the first to start a conversation!", which is not true ([What each role can do](../cross-cutting/permissions.md#what-each-role-can-do)). | A private window signed out, plus a player account that can confirm messages exist. | 1. As the player, confirm the board holds messages.<br>2. In the private window, open `/message-board`.<br>3. Read the middle of the page word for word. | Record what happens. Expected: the empty state claims there are no messages, which is a positive claim about the world and is false. **(suspected bug)** | — |
| PERM-03 | P2 | visitor | A separate bar at the bottom of the board says "Sign in to post messages" ([What each role can do](../cross-cutting/permissions.md#what-each-role-can-do)). | Private window, signed out, on `/message-board`. | 1. Scroll to the bottom of the page.<br>2. Read the bar and its button. | "Sign in to post messages" with a Sign In button. On a phone see PHONE-20, where the same bar is probably unreachable. | — |
| PERM-04 | P1 | visitor | Anyone, signed out included, can open the Pending Scores card and report a score for any match ([What each role can do](../cross-cutting/permissions.md#what-each-role-can-do)). | Private window signed out; an admin in another browser; a match missing a result. | 1. Open `/` signed out and find the Pending Scores card.<br>2. Open it, choose any match, and read the form.<br>3. Submit a clearly marked test report.<br>4. As the admin, open the Pending section and look for it. | The card and the form are offered to a signed-out visitor, and the report reaches the admin queue marked unverified. Tell the admin to discard it. | — |
| PERM-05 | P2 | player | A signed-in reporter's typed name and team are replaced by their profile name and approved team and the report is marked verified; a signed-out one is kept as typed and marked unverified; nothing on the form explains either ([What each role can do](../cross-cutting/permissions.md#what-each-role-can-do)). | A player with an approved membership, an admin in another browser, and a test match. | 1. As the player, type a deliberately wrong name and team into the report form and submit.<br>2. As the admin, read the stored report.<br>3. Repeat signed out and compare. | The signed-in report carries the profile name and approved team and a verified mark; the signed-out one carries what was typed and no mark. The form says nothing about either. | — |
| PERM-06 | P2 | visitor | Anyone can sign up for the blind draw and nobody but an admin can read the list back, not even the person who just signed up ([What each role can do](../cross-cutting/permissions.md#what-each-role-can-do)). | Private window signed out, an admin in another browser, blind draw signups open. | 1. Sign up with a clearly marked test name.<br>2. Read everything the visitor is shown afterwards.<br>3. As the admin, find the entry in the Blind Draw section and remove it. | The visitor sees a confirmation message and a count, and never the list. Only the admin can read or remove the entry. | — |
| PERM-07 | P2 | visitor | Signing up twice gives two entries and no way to see or remove either ([What each role can do](../cross-cutting/permissions.md#what-each-role-can-do)). | As PERM-06. | 1. Sign up twice with the same test name.<br>2. Read what the visitor is shown.<br>3. As the admin, count the entries and remove both. | Nothing warns the visitor and nothing offers a way back. Two entries reach the admin's list. | — |
| PERM-08 | P1 | player | A control a role may not use is **absent**, not disabled, so "not allowed" cannot be told from "does not exist" ([The four shapes a refusal takes](../cross-cutting/permissions.md#the-four-shapes-a-refusal-takes)). | Signed in, not an admin, not a member of the team on screen. | 1. Open another team's page and look for an Edit control.<br>2. Open a completed match and look for a reopen control.<br>3. Open an open match's live page and look for the score grids.<br>4. Note whether any of them is present but greyed. | None of them is drawn at all. Nothing is disabled and nothing explains the absence. | — |
| PERM-09 | P2 | visitor | Where a read is refused, no error is raised: the query returns nothing and the page shows its ordinary empty state ([The four shapes a refusal takes](../cross-cutting/permissions.md#the-four-shapes-a-refusal-takes)). | Private window, signed out. | 1. Open `/message-board` and read the middle of the page.<br>2. Open `/my-team` and read it.<br>3. Look for any error, any red bar, and any sign-in prompt in the content area. | Both show a plain empty state that reads as a fact about the world. No error is raised in either. | — |
| PERM-10 | P2 | player | A refused write is reported as that feature's ordinary error toast, with no hint that permission was the reason ([The four shapes a refusal takes](../cross-cutting/permissions.md#the-four-shapes-a-refusal-takes)). | A player with an **unapproved** membership, on their team's open match. | 1. Reach any write the browser still offers.<br>2. Attempt it and read the toast word for word.<br>3. Compare with the same feature's toast when the network is down. | The two are indistinguishable. The database's own message is discarded and nothing names permission. | — |
| PERM-11 | P2 | admin | On a guarded route the page does not render until the session **and** the profile have both settled ([Arrive](../cross-cutting/permissions.md#arrive)). | Admin, signed in. | 1. From `/schedule`, navigate to `/admin` in-app and time the spinner.<br>2. Reload `/admin` from cold and time it again. | "Checking access..." both times: near-instant on the in-app navigation because the profile is already loaded, a real wait on the cold load. | — |
| PERM-12 | P2 | admin | On every other route nothing is checked, and admin controls appear the instant the profile does, without flickering ([Arrive](../cross-cutting/permissions.md#arrive)). | Admin, a cold load of `/playoffs` on a throttled connection. | 1. Reload `/playoffs` and watch the admin controls from the first paint.<br>2. Note whether they ever appear and then disappear, or appear once. | The page renders immediately with no access check. The admin controls appear once, when the profile lands, and do not flicker. | — |
| PERM-13 | P1 | admin, second browser | A role changed under a user mid-session never reaches the browser: nothing subscribes to profiles or memberships ([While editing](../cross-cutting/permissions.md#while-editing)). | Two admin accounts, A sitting on `/admin`; and separately a player with an approved membership sitting on `/my-team`. | 1. As B, remove A's admin flag and watch A for three minutes.<br>2. As an admin, approve or withdraw the player's membership and watch their screen for three minutes. | Neither screen changes on its own. The controls stay exactly as they were until something causes a refetch, and in between the browser and the database disagree. | — |
| PERM-14 | P1 | admin, offline | A failed profile read demotes an admin silently and turns them away with "Access Denied" ([Open questions](../cross-cutting/permissions.md#open-questions-and-verification)). | Admin, signed in, at `/schedule`. | 1. Throttle or block the profile request only, if the network panel allows it; otherwise go offline.<br>2. Reload `/admin`.<br>3. Read every message on screen and note where you land. | Record what happens. Expected: a real admin is told "You do not have admin privileges" for what is a network problem, with no toast at all on a reload. **(suspected bug)** | — |
| PERM-15 | P2 | player | Nothing is recorded by being refused: there is no audit a user can see and no limit on trying again ([Leave without changing anything](../cross-cutting/permissions.md#leave-without-changing-anything)). | Signed in, not an admin. | 1. Visit `/admin` ten times in a minute.<br>2. Count the toasts and note whether anything changes on the tenth attempt.<br>3. Look anywhere in the app for a record of the attempts. | The same one toast each time, no rate limiting, and no record anywhere. | — |
| PERM-16 | P2 | player | The redirect replaces the guarded route in history, so Back after being bounced goes to whatever was before it ([Cancel and interrupt](../cross-cutting/permissions.md#cancel-and-interrupt)). | Signed in, not an admin, on `/schedule`. | 1. Type `/admin` and let the redirect happen.<br>2. Press the browser Back button once and read the address. | You land on `/schedule`, not back on `/admin`. The guarded route is not in the history. | — |
| PERM-17 | P2 | player | Leaving a guarded route while it still says "Checking access..." simply unmounts it: no redirect and no toast ([Cancel and interrupt](../cross-cutting/permissions.md#cancel-and-interrupt)). | Signed in, not an admin, connection throttled hard. | 1. Type `/admin`.<br>2. While "Checking access..." is on screen, click an in-app link away.<br>3. Watch the new page for ten seconds. | You leave immediately. No redirect fires and no "Access Denied" toast arrives afterwards. | — |
| PERM-18 | P2 | player | A refusal toast appears on whatever page the user is now on ([Cancel and interrupt](../cross-cutting/permissions.md#cancel-and-interrupt)). | Signed in, not an admin. | 1. Type `/admin` and let the redirect finish.<br>2. Note which page the toast is drawn over. | The toast is drawn over the home page, not over `/admin`, with nothing to say where it came from. | — |
| PERM-19 | P2 | player | An expired session and a permission refusal are indistinguishable ([Cancel and interrupt](../cross-cutting/permissions.md#cancel-and-interrupt)). | Signed in; sign out of the same account in a second browser to expire the session. | 1. Attempt a write and read the toast.<br>2. Attempt the same write while genuinely unauthorised and read the toast.<br>3. Compare word for word. | The two messages are the same. Nothing mentions the session and nothing mentions permission. | — |
| PERM-20 | P1 | player | A player with approved memberships of two teams breaks the membership read and loses every member ability at once ([Edge cases](../cross-cutting/permissions.md#edge-cases)). | Ask the league whether any account holds two approved memberships. Note that REQ-21 refuses a second approval from the admin screen, so the state may be unreachable. | 1. If such an account exists, sign in as it and open `/my-team`.<br>2. Read the middle of the page.<br>3. Open one of their teams' open matches and look for the score grids. | Record what happens. Expected: "Failed to load team membership" and no member abilities at all. Record `blocked` if no such account exists. **(suspected bug)** | — |
| PERM-21 | P2 | admin | An admin cannot delete another person's message; the board has no moderation ([Edge cases](../cross-cutting/permissions.md#edge-cases)). | Admin, on `/message-board`, with messages from other people. | 1. Read the controls on another person's message.<br>2. Read the controls on your own.<br>3. Look for any moderation control anywhere on the board. | The delete control is drawn only on your own messages. Nothing anywhere lets an admin remove somebody else's. | — |
| PERM-22 | P2 | player | A member can rename their own team and change its image, and cannot set that team's timeslot preferences **(destructive)** ([Edge cases](../cross-cutting/permissions.md#edge-cases)). | A player with an approved membership. | 1. Open their team's page and find the Edit control.<br>2. Read what the edit form offers — name, image, and what else.<br>3. Look for anything that sets a timeslot.<br>4. **Close the form without saving; the team name is what everyone sees in standings, the schedule, and history.** | The name and the image are editable by the member. No timeslot control is offered; those are admin-only writes on a guarded route. | — |
| PERM-23 | P2 | player | A completed match is read-only for everybody, admins included, until an admin reopens it ([Edge cases](../cross-cutting/permissions.md#edge-cases)). | An admin and a member, both on a completed match's live page. | 1. As the member, look for any scoring control.<br>2. As the admin, look for any scoring control.<br>3. Note that the admin's only extra control is the reopen. | Neither can score. The completion is tested before the role is. | — |
| PERM-24 | P1 | visitor | `/my-team` signed out renders with the heading, the blurb, and "No Teams Available — There are no teams to join at the moment." while teams do exist ([Edge cases](../cross-cutting/permissions.md#edge-cases)). | Private window, signed out, with teams known to exist on `/teams`. | 1. Confirm `/teams` lists teams.<br>2. Open `/my-team` in the same private window.<br>3. Read the middle of the page word for word, and look for a sign-in prompt. | Record what happens. Expected: an empty state that is a false claim about the world, with nothing asking the visitor to sign in. **(suspected bug)** | — |
| PERM-25 | P2 | admin, second browser | Admin revoked while the page is open leaves every admin control on screen and every admin write failing with a generic message ([Edge cases](../cross-cutting/permissions.md#edge-cases)). | Two admin accounts, A on `/playoffs` with a bracket open. | 1. As B, remove A's admin flag.<br>2. As A, without reloading, read the controls.<br>3. As A, press one and read the message. | Every control is still drawn. The write fails with that control's ordinary error, and nothing says permission was the reason. | — |
| PERM-26 | P2 | browser | The top navigation bar has no admin link; the only route in is the user menu or the address ([Edge cases](../cross-cutting/permissions.md#edge-cases)). | Admin, signed in, on `/`. | 1. Read every entry in the top navigation.<br>2. Open the user menu and read it.<br>3. Repeat at a phone width. | No admin entry in the navigation at any width. "Admin Panel" is in the user menu, which on a phone sits beside the hamburger. | — |
| PERM-27 | P2 | player | The "Access Denied" toast is shown once per visit to a guarded route, not once per render ([Interactions with other systems](../cross-cutting/permissions.md#interactions-with-other-systems)). | Signed in, not an admin. | 1. Type `/admin` and count the toasts over ten seconds.<br>2. Type `/timeslots` and count again. | Exactly one each time. | — |
| PERM-28 | P2 | player | No role, team, or permission is ever in the URL, and the destination remembered across a sign-in redirect does not survive a reload of `/auth` ([Interactions with other systems](../cross-cutting/permissions.md#interactions-with-other-systems)). | Private window, signed out. | 1. Type `/admin` and read the address you land on.<br>2. Reload `/auth`.<br>3. Sign in as an admin and note where you land. | The address carries no role and no destination. After the reload the remembered destination is gone and you land on `/` rather than `/admin`. | — |
| PERM-29 | P2 | player, second browser | A scorer whose membership is revoked mid-match keeps the score grids and finds out at the next save **(destructive)** ([Cancel and interrupt](../cross-cutting/permissions.md#cancel-and-interrupt)). | A **scratch match**, a member scoring it, and an admin in a second browser. | 1. Start scoring the scratch match as the member.<br>2. As the admin, remove that membership.<br>3. As the member, without reloading, enter a round and save. | The grids stay on screen. The save fails with the feature's ordinary message. Record `blocked` without a scratch match. | — |
| PERM-30 | P2 | player | Every refusal that redirects moves the user to a different page and writes nothing about the move into a live region ([Interactions with other systems](../cross-cutting/permissions.md#interactions-with-other-systems)). | Signed in, not an admin, the page's live regions visible in the element inspector. | 1. Note the text of every live region on the page you start from.<br>2. Type `/admin` and let the redirect happen.<br>3. Read every live region on the page you land on. | The route live region names the home page, as it would for any navigation. Nothing anywhere states that a refusal is what moved you. | — |
| PERM-31 | P2 | player | An unapproved membership grants nothing ([What each role can do](../cross-cutting/permissions.md#what-each-role-can-do)). | A player with a pending, unapproved membership. | 1. Open `/my-team` and read what it says.<br>2. Open one of that team's open matches in live scoring.<br>3. Open the team's own page and look for an Edit control. | Waiting for approval, no scoring controls, and no Edit. The membership is not a column in the role table for a reason. | — |
| PERM-32 | P2 | visitor | Nearly everything in the product is readable by anyone, so roles decide writes rather than reads ([Summary](../cross-cutting/permissions.md#summary)). | Private window, signed out. | 1. Open `/teams`, `/schedule`, `/stats`, `/insights`, `/compare`, `/playoffs`, `/history`, `/help` in turn.<br>2. Note anything that refuses to render or shows a sign-in prompt in the content area. | Every one renders its content signed out. Only the message board and `/my-team` behave otherwise, and both do so by showing a false empty state rather than a refusal. | — |
| PERM-33 | P2 | admin | Only an admin sees hidden teams in listings ([What each role can do](../cross-cutting/permissions.md#what-each-role-can-do)). | Admin in one browser, a private window signed out, a team known to be hidden. | 1. As the admin, find the team in the dashboard's Manage Teams list.<br>2. As the visitor, search `/teams` and `/compare` for it. | Listed for the admin and absent for the visitor, with no message either way. | — |
| PERM-34 | P2 | player | A member may add a player to a roster during setup and finalise a live match, and may not reopen a completed one ([What each role can do](../cross-cutting/permissions.md#what-each-role-can-do)). | A member on a **scratch match's** live page. | 1. In game setup, add a player by typing a name and confirm they appear on the roster.<br>2. Score to a decision and confirm the finalise control is offered.<br>3. After finalising, look for a reopen control. | The first two are offered to a member. The reopen is not. Record `blocked` without a scratch match. | — |
| PERM-35 | P2 | player | Returning to the tab refetches stale data, which is the usual way a stale role finally corrects itself — with no message ([Cancel and interrupt](../cross-cutting/permissions.md#cancel-and-interrupt)). | A player whose membership was approved in another browser while they watched. | 1. Confirm the player's screen has not changed on its own.<br>2. Switch away for six minutes and come back.<br>3. Watch the controls. | The new abilities appear on return, silently, with nothing to say what changed or when. | — |
| PERM-36 | P2 | player | Escape closes a confirmation dialog in front of a privileged action and does not abort a refused write already sent ([Cancel and interrupt](../cross-cutting/permissions.md#cancel-and-interrupt)). | A member on a confirmation dialog in live scoring, connection throttled. | 1. Open a confirmation and press Escape; confirm nothing is written.<br>2. Open it again, confirm, and press Escape while the request is in flight.<br>3. Read the result. | The first press abandons the action. The second changes nothing: no request in the app can be cancelled once sent. | — |

Not checkable by hand:

- Whether the database refuses any write the browser offers, in normal use.
  Every disagreement in the document was read from the policies and the
  components rather than observed, and confirming one means making a request
  outside the browser.
- Whether `is_admin` is the only column that grants admin anywhere. Needs the
  migrations, not the UI.
- Whether the "Access Denied" toast is ever actually heard, or whether the
  redirect completes first and replaces it. The device list in
  [`README.md`](README.md) has no screen reader in it; see
  [`accessibility.md`](../cross-cutting/accessibility.md).
- The **Modifiers** table this document drops. Its axis is the user's role,
  which is the subject of every item above; there is nothing left over to check.

## cross-cutting/errors-and-offline.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ERR-01 | P1 | offline | Nothing anywhere says the user is offline: the same words appear as for any other failure ([Offline](../cross-cutting/errors-and-offline.md#offline)). | Signed in, on any page with a write control. | 1. Attempt a write while online but forced to fail, and read the toast.<br>2. Go offline and attempt the same write.<br>3. Compare the two word for word, and look for any banner, badge, or disabled control. | The messages are identical. There is no offline banner, no badge, no disabled-while-offline control, and no check before a form is opened. | — |
| ERR-02 | P1 | offline | A page whose code has not downloaded hangs on "Loading page..." for ever: no timeout, no error, no Try Again ([Edge cases](../cross-cutting/errors-and-offline.md#edge-cases)). | A fresh session with `/` loaded, then the network cut. | 1. Load `/` and wait for it to settle.<br>2. Go offline.<br>3. Navigate to `/playoffs`, which is not preloaded.<br>4. Wait two minutes. | The spinner stays. Nothing times out, nothing errors, and nothing offers a retry. | — |
| ERR-03 | P2 | offline | An ordinary read is retried once, with no backoff, and then the page falls back to its own state ([Retrying, and where it differs](../cross-cutting/errors-and-offline.md#retrying-and-where-it-differs)). | Signed in, network panel open, a page whose read can be made to fail. | 1. Make the read fail and count the attempts in the network panel.<br>2. Time the gap between them.<br>3. Read what the page shows afterwards. | Two attempts in total, back to back, then the page's own empty or error state. | — |
| ERR-04 | P2 | offline | Writes are never retried ([Retrying, and where it differs](../cross-cutting/errors-and-offline.md#retrying-and-where-it-differs)). | Signed in, offline, network panel open, any form. | 1. Submit the form.<br>2. Count the requests in the network panel. | Exactly one attempt. A failed write is a failed write. | — |
| ERR-05 | P1 | offline | The server's own reason reaches the browser and is then replaced by the feature's fixed sentence ([The life of one failing request](../cross-cutting/errors-and-offline.md#the-life-of-one-failing-request)). | Signed in, a write that the server refuses for a stateable reason — the timeslot 9:30 PM case in SLOTADMIN-13 is the reliable one. | 1. Trigger the refusal with the network panel open.<br>2. Read the server's response body.<br>3. Read the toast. | The response carries a real reason. The toast carries the feature's own sentence instead. The error object keeps the code and the hint and nothing shows them. | — |
| ERR-06 | P1 | offline | The network-error helper sentence exists in the code and has never been shown to a user ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Signed in, offline. | 1. Attempt at least a dozen different writes across the app while offline — a message, a score report, a contact request, an admin write, a team edit.<br>2. Record every toast word for word.<br>3. Look for "Network error. Please check your connection and try again." | Record what happens. Expected: that sentence never appears once, on any path. **(suspected bug)** | — |
| ERR-07 | P2 | browser | Nothing is cancelled once sent: leaving the page, pressing Escape, or navigating away does not abort a request ([The life of one failing request](../cross-cutting/errors-and-offline.md#the-life-of-one-failing-request)). | Signed in, connection throttled hard, any form. | 1. Submit the form and immediately navigate away.<br>2. Watch the network panel until the request finishes.<br>3. Return and check whether the write landed. | The request completes and the write lands. The toast appears over the new page. | — |
| ERR-08 | P1 | browser | Only one toast exists at a time, so an operation that fails several times shows only the last failure ([The life of one failing request](../cross-cutting/errors-and-offline.md#the-life-of-one-failing-request)). | Signed in, offline. | 1. Trigger two writes that will fail within a second of each other.<br>2. Watch the toast area continuously and count what is on screen. | One toast. The first is gone before it can be read. | — |
| ERR-09 | P2 | slow | The two timeslot reads retry **twice** with growing delays, and are the only reads that ask the browser whether it is online before polling again ([Retrying, and where it differs](../cross-cutting/errors-and-offline.md#retrying-and-where-it-differs)). | Admin, the Timeslots section, network panel open, the list read made to fail. | 1. Make the list read fail and count the attempts.<br>2. Time the gaps between them.<br>3. Go offline and watch whether the poll keeps firing. | Three attempts with a growing gap, and no polling at all while the browser reports itself offline. Compare with ERR-03. | — |
| ERR-10 | P2 | phone, offline | A realtime channel reconnects by itself with growing delays and refetches on every reconnection ([Retrying, and where it differs](../cross-cutting/errors-and-offline.md#retrying-and-where-it-differs)). | A phone on a live-scoring page, with aeroplane mode available. | 1. Note the connection indicator.<br>2. Turn aeroplane mode on for ten seconds, then off, timing the reconnection.<br>3. Repeat with a two-minute drop and time it again. | The channel rebuilds itself, waiting longer each time up to about thirty seconds, and the screen refetches on every reconnection rather than assuming nothing was missed. | — |
| ERR-11 | P1 | offline | There is no offline write queue anywhere: a round saved with no signal is not queued and is not auto-sent when the signal returns ([Offline](../cross-cutting/errors-and-offline.md#offline)). | A **scratch match** on a phone, with a member signed in. | 1. Go offline.<br>2. Enter a round and save.<br>3. Come back online and wait five minutes without pressing anything.<br>4. Reload and read the round list. | The save fails at once and the tapped scores stay on screen. Nothing is queued, and nothing is sent when the connection returns. After the reload the grids are empty and the round is gone. Record `blocked` without a scratch match. | — |
| ERR-12 | P2 | offline | Data already fetched stays on screen and looks current, every signed-in control is still drawn, and the user finds out by pressing something ([Offline](../cross-cutting/errors-and-offline.md#offline)). | Signed in, several pages already visited. | 1. Go offline.<br>2. Navigate between pages already fetched and read the numbers.<br>3. Read the header and every write control. | Everything looks alive: real numbers, a signed-in header, and every control drawn. Nothing warns until a press fails. | — |
| ERR-13 | P2 | offline | The installed home-screen app opens offline and nothing works ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | A phone with the app installed to the home screen. | 1. Turn aeroplane mode on.<br>2. Open the installed app from the home screen.<br>3. Read what is on screen and try one read and one write. | Record what happens. Expected: the shell opens and looks alive while nothing behind it works. | — |
| ERR-14 | P3 | phone | On a narrow screen a toast is full width at the **top**; from 640 pixels up it is bottom right and at most 420 pixels wide ([Modifiers](../cross-cutting/errors-and-offline.md#modifiers)). | Any role, a resizable window, a toast to hand. | 1. Raise a toast at 390 pixels wide and note its position and width.<br>2. Raise one at 800 pixels and note both again.<br>3. Measure the wide one. | Top and full width below 640 pixels; bottom right and at most 420 pixels above it. | — |
| ERR-15 | P2 | browser | A development build shows the underlying error message on both error screens and a published build does not ([Modifiers](../cross-cutting/errors-and-offline.md#modifiers)). | Both builds available, and a way to force a route error in each. | 1. Force a route error on the dev server and read the screen.<br>2. Do the same on the published build.<br>3. Compare. | The dev build shows the message; the published build hides it. Record `blocked` if only one build is available. | — |
| ERR-16 | P2 | browser | A failure toast survives navigation and appears over an unrelated page, with no clue which page it came from ([Cancel and interrupt](../cross-cutting/errors-and-offline.md#cancel-and-interrupt)). | Signed in, connection throttled, any form. | 1. Submit and immediately navigate two pages away.<br>2. Watch the toast area on the new page. | The toast arrives over the unrelated page and names only the feature, not the page. | — |
| ERR-17 | P2 | browser | After a reload nothing tells the user whether a request landed, so a retry can duplicate the write ([Cancel and interrupt](../cross-cutting/errors-and-offline.md#cancel-and-interrupt)). | Signed in, connection throttled hard, a **scratch record** to write. | 1. Submit, then reload before the answer arrives.<br>2. Read the page for any statement about the in-flight write.<br>3. Check whether the record exists. | Nothing anywhere says whether it landed. A retry made in ignorance writes it a second time. Record `blocked` without a scratch record. | — |
| ERR-18 | P1 | offline | A failed read and an empty list look identical on any page that falls back to its empty state ([Edge cases](../cross-cutting/errors-and-offline.md#edge-cases)). | Private window, the database made unreachable. | 1. Load `/teams` with the read failing and read the middle of the page.<br>2. Compare with what `/teams` shows for a division that genuinely has no teams. | The two are indistinguishable. The empty state is a positive claim and is sometimes false. | — |
| ERR-19 | P2 | offline | The standings page prints the raw error message under its alert, which happens on that one page and nowhere else ([Edge cases](../cross-cutting/errors-and-offline.md#edge-cases)). | Any role, `/stats`, the read made to fail. | 1. Load `/stats` with the read failing.<br>2. Read the alert and everything under it.<br>3. Do the same on `/teams` and `/schedule` and compare. | "There was an error loading the statistics data" with the database's own wording under it. No other page shows a raw message. | — |
| ERR-20 | P2 | browser | A rate-limited or over-length contact message is told to try again, which cannot work ([Edge cases](../cross-cutting/errors-and-offline.md#edge-cases)). | Any role, `/contact`. | 1. Submit a message longer than 5000 characters.<br>2. Read the toast.<br>3. Follow its advice and submit the same message again. | "Failed to send message. Please try again." — advice that cannot succeed, because the message will always be too long. | — |
| ERR-21 | P2 | offline | Two writes failing together show one toast; the first is replaced ([Edge cases](../cross-cutting/errors-and-offline.md#edge-cases)). | Signed in, offline, a screen with two independent write controls. | 1. Press both within a second.<br>2. Count the toasts on screen. | One. See ERR-08 — this is the same rule seen from the failure side. | — |
| ERR-22 | P3 | browser | The route error screen's Home button is a full page load, so it discards everything including a toast the user had not finished reading ([Edge cases](../cross-cutting/errors-and-offline.md#edge-cases)). | A page forced to fail its first render, with the network panel open. | 1. Reach the route error screen with a toast on screen.<br>2. Press **Home** and watch the network panel and the toast. | The whole app reloads rather than navigating in place, and the toast is gone. | — |
| ERR-23 | P2 | offline | An offline user can fill in a long form and lose all of it at submit ([Edge cases](../cross-cutting/errors-and-offline.md#edge-cases)). | Any role, `/contact`, offline. | 1. Fill every field with several paragraphs.<br>2. Submit.<br>3. Read the form after the failure. | The submit fails and the form is the only place the text ever was. Nothing offers a draft, a copy, or a retry that keeps it. Record exactly what survives. | — |
| ERR-24 | P2 | browser, second browser | Two people writing the same record outside live scoring overwrite each other with no conflict, no warning, and no error ([Cancel and interrupt](../cross-cutting/errors-and-offline.md#cancel-and-interrupt)). | Two browsers, both able to write the same **scratch record**. | 1. Open it in both and change it to different values.<br>2. Save in A, then in B.<br>3. Reload both. | B's value stands. Neither is warned and nothing records that two writes happened. Record `blocked` without a scratch record. | — |
| ERR-25 | P2 | phone, offline | A brief realtime drop is invisible because reconnecting refetches; only live scoring shows the connection's state at all ([Interactions with other systems](../cross-cutting/errors-and-offline.md#interactions-with-other-systems)). | A phone on a live-scoring page, and a second browser on `/message-board`. | 1. Drop the connection for five seconds on the live-scoring page and watch the indicator and the score.<br>2. Do the same on the message board, which also holds a channel, and watch for any indicator. | Live scoring shows its state changing. The message board shows nothing at all, even though its channel is down. | — |
| ERR-26 | P2 | browser | Field rules run in the browser and appear under the field; server refusals never appear under a field ([Interactions with other systems](../cross-cutting/errors-and-offline.md#interactions-with-other-systems)). | Any role, a form with field validation — `/contact` will do. | 1. Submit it empty and note where the messages appear.<br>2. Force a server refusal and note where that message appears. | Field rules under their fields. The server refusal as a toast, never under a field. | — |
| ERR-27 | P2 | offline | A rollback is announced only by the failure toast; the value changing back carries no other mark ([Interactions with other systems](../cross-cutting/errors-and-offline.md#interactions-with-other-systems)). | Signed in, a screen with an optimistic write — a message board reaction will do. | 1. Go offline and make the optimistic change.<br>2. Watch the value and the toast area. | The value shows the new state, then reverts. The toast is the only statement that anything failed. | — |
| ERR-29 | P2 | offline | No failure message anywhere mentions the network or the session ([Modifiers](../cross-cutting/errors-and-offline.md#modifiers)). | Signed in, offline; then signed in with an expired session. | 1. Record ten failure messages while offline.<br>2. Record the same ten with an expired session.<br>3. Read them for any mention of the network, the connection, or signing in. | None mentions either. A refused-by-permission write, a broken connection, and an expired session all produce the same generic sentence. | — |
| ERR-30 | P2 | browser | No error state is in the URL, so a failure cannot be linked to, bookmarked, or reproduced from an address ([Interactions with other systems](../cross-cutting/errors-and-offline.md#interactions-with-other-systems)). | Any role, a page in a failed state. | 1. Reach any failure state and read the address bar.<br>2. Copy the URL into a second browser. | The address is unchanged by the failure. The second browser loads the page normally. | — |
| ERR-31 | P3 | browser | A toast can be dismissed from the keyboard and the route error screen's three buttons are reachable by Tab ([Modifiers](../cross-cutting/errors-and-offline.md#modifiers)). | A toast on screen; and separately, a page forced to fail. | 1. Tab to the toast and dismiss it by key.<br>2. On the route error screen, Tab through and note the order of Try Again, Go Back, and Home. | The toast dismisses by key. All three buttons are reachable in order. There is no global Escape handler. | — |
| ERR-32 | P1 | offline | What `/` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the hero cards, the Pending Scores card, and the next-match strip — an empty state or an error, not a blank area. | — |
| ERR-33 | P1 | offline | What `/teams` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/teams` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the division groups and the team cards — an empty state or an error, not a blank area. | — |
| ERR-34 | P1 | offline | What `/teams/:teamId` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/teams/:teamId` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the record, the roster, and the fixture list — an empty state or an error, not a blank area. | — |
| ERR-35 | P1 | offline | What `/schedule` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/schedule` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the week groups and the match cards — an empty state or an error, not a blank area. | — |
| ERR-36 | P1 | offline | What `/stats` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/stats` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the standings table — an empty state or an error, not a blank area. The 2026-08-25 automated pass found this page rendered **no message at all** between the navigation bar and the footer. | — |
| ERR-37 | P1 | offline | What `/playoffs` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/playoffs` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the division cards and any bracket — an empty state or an error, not a blank area. | — |
| ERR-38 | P1 | offline | What `/history` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/history` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the archived seasons — an empty state or an error, not a blank area. | — |
| ERR-39 | P1 | offline | What `/message-board` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/message-board` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the message list — an empty state or an error, not a blank area. | — |
| ERR-40 | P1 | offline | What `/my-team` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/my-team` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the membership state or the join prompt — an empty state or an error, not a blank area. | — |
| ERR-41 | P1 | offline | What `/help` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/help` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of its static content, which needs no read at all — an empty state or an error, not a blank area. | — |
| ERR-42 | P1 | offline | What `/contact` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/contact` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the form, which needs no read at all — an empty state or an error, not a blank area. | — |
| ERR-43 | P1 | offline | What `/compare` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/compare` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of the two team pickers — an empty state or an error, not a blank area. | — |
| ERR-44 | P1 | offline | What `/insights` shows when its first read fails is not settled; this is the largest gap in the document ([Open questions](../cross-cutting/errors-and-offline.md#open-questions-and-verification)). | Private window, signed out, the database made unreachable from the browser. | 1. Load `/insights` with the read failing.<br>2. Wait thirty seconds.<br>3. Record exactly what appears between the navigation bar and the footer.<br>4. Note whether it is an empty state, a red bar, a toast, or nothing at all. | Record what happens. Something should be shown in place of its charts and tables — an empty state or an error, not a blank area. | — |
| ERR-45 | P2 | browser | The app fails to start with a dark screen naming two environment variables, and it is the only screen in the product with no navigation on it ([The four things that can fail](../cross-cutting/errors-and-offline.md#the-four-things-that-can-fail)). | A build deliberately published without its configuration, if one can be produced safely. | 1. Load it.<br>2. Read the screen and look for any navigation. | "App configuration missing", the two variable names, and instructions to re-publish, with nothing else on the page. Record `blocked` if no such build can be produced. | — |

Not checkable by hand:

- Whether a failed page-code download ever recovers on its own, and how long a
  user waits before giving up on the "Loading page..." spinner. Both need a
  connection that fails in one specific way for a long time.
- Whether "Try Again" on the route error screen recovers or re-throws
  immediately. Needs a reproducible render failure.
- Whether a failure toast and the shared red error bar are actually
  announced, and whether a page moving from loading to an empty state says
  anything. The device list in [`README.md`](README.md) has no screen reader
  in it, so "was it announced" cannot be recorded from this checklist; see
  [`accessibility.md`](../cross-cutting/accessibility.md).
- That every failed read and every failed write is counted for monitoring, and
  that non-network errors are reported with a stack. Nothing about it is visible
  to a user; see
  [`what-the-league-sees.md`](../cross-cutting/what-the-league-sees.md).
- The five named phases this document drops. A failure has no editing phase, and
  the life of one failing request covers the same ground; nothing is left over.

## cross-cutting/on-a-phone.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PHONE-01 | P1 | phone | The app has exactly one JavaScript breakpoint, at 768 pixels wide ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | A resizable desktop browser on `/`. | 1. Set the window to 767 pixels wide and note the navigation.<br>2. Set it to 768 and note it again.<br>3. Step through 480, 640, 1024, 1280 and 1536 and note whether anything else swaps. | The layout changes once, at 768. The other widths re-flow the same content and swap no navigation. | — |
| PHONE-02 | P2 | phone | Below 768 every navigation link is behind a hamburger button; from 768 up the links sit across the top bar ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | Browser at 390 x 844 with touch emulation. | 1. Read the top bar and list what is on it.<br>2. Open the hamburger and list what is inside.<br>3. Widen past 768 and list the top bar again. | Narrow: league name, user menu, bell, theme button, hamburger. Wide: the links themselves across the bar and no hamburger. | — |
| PHONE-03 | P2 | phone | A fixed tab bar sits at the bottom of the screen with exactly four tabs ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | Browser at 390 x 844 with touch emulation. | 1. Read the tab bar and name each tab.<br>2. Scroll the page and watch whether the bar moves. | Standings, Schedule, Teams, Playoffs, and nothing else. The bar stays fixed while the page scrolls. | — |
| PHONE-04 | P2 | browser | From 768 up there is no bottom bar, and a second navigation row sits under the header with three entries ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | Desktop browser at 1024 pixels. | 1. Look for a bottom bar.<br>2. Read the second row under the header. | No bottom bar at all. The second row holds Standings, Schedule and Teams — three, not the tab bar's four. | — |
| PHONE-05 | P1 | browser | Cmd/Ctrl+K opens a command palette that jumps to seven pages or to a team by name, and it exists only from 768 pixels up ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | A resizable browser on `/`. | 1. At 1024 pixels press Cmd/Ctrl+K and read what opens.<br>2. Count the pages it offers and type a team name.<br>3. Narrow to 390 pixels and press the same keys. | Wide: a palette listing seven pages and matching teams by name, and the browser's own use of that key is suppressed. Narrow: nothing happens at all. | — |
| PHONE-06 | P2 | phone | There is no Search button and no search anywhere below 768 pixels ([Edge cases](../cross-cutting/on-a-phone.md#edge-cases)). | Browser at 390 x 844 with touch emulation. | 1. Read the top bar for a Search control.<br>2. Open the hamburger and read it for one.<br>3. Read the bottom tab bar. | No search control anywhere on a narrow screen — not a disabled one, none. | — |
| PHONE-07 | P2 | phone | Pages get five extra lines of bottom padding plus the phone's own safe area, so the tab bar never covers the last line ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | A real phone with a home indicator, or a browser at 390 x 844. | 1. Open a long page and scroll to the very bottom.<br>2. Read the last line of content against the tab bar. | The last line clears the tab bar with room to spare. Nothing is hidden behind it. | — |
| PHONE-08 | P3 | phone | Toasts fill the width at the top of the screen on a narrow window ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | Browser at 390 x 844, a toast to hand. | 1. Raise a toast and note where it is drawn and how wide it is.<br>2. Note what it covers while it is there. | Full width, at the top, covering the top bar and the page heading — **not** the submit button at the bottom. | — |
| PHONE-09 | P2 | phone | The teams list defaults to grouped by division on a phone and to one list on a desktop ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | A resizable browser on `/teams`, loaded fresh at each width. | 1. Load `/teams` at 390 pixels and read the default grouping.<br>2. Load it fresh at 1024 pixels and read it again. | Grouped by division on the narrow screen, one list on the wide one, with no choice remembered between them. | — |
| PHONE-10 | P2 | phone | Several stats and standings tables render as stacked cards on a narrow screen ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | A resizable browser. | 1. Open `/stats`, `/insights`, `/compare` and `/history` at 390 pixels and note which render as cards.<br>2. Open the same four at 1024 pixels and note which render as tables.<br>3. Write down the list. | The same data appears in both shapes. Record which pages branch — the document does not enumerate them. | — |
| PHONE-11 | P2 | phone | One dialog in the whole app — playoff team divisions — opens as a bottom drawer; every other dialog is a centred modal ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | Browser at 390 x 844, on `/playoffs`. | 1. Open the playoff team divisions dialog and note how it appears.<br>2. Open two other dialogs elsewhere in the app at the same width. | The first slides up from the bottom as a drawer. The others are centred modals, including the live scoring confirmations. | — |
| PHONE-12 | P2 | phone | Wide content scrolls sideways inside its own box, and the page itself never scrolls sideways ([What changes at 768 pixels](../cross-cutting/on-a-phone.md#what-changes-at-768-pixels)). | Browser at 390 x 844 with touch emulation. | 1. Open a page with a wide table and drag the table sideways.<br>2. Drag the page itself sideways.<br>3. Repeat on `/stats`, `/schedule`, `/history` and `/insights`. | The table scrolls within its box. The page body does not move sideways on any of them. | — |
| PHONE-13 | P1 | phone | Rotating a large phone past 768 pixels swaps the bottom tab bar for the desktop navigation row while the user is looking at the page ([Crossing the line while using the app](../cross-cutting/on-a-phone.md#crossing-the-line-while-using-the-app)). | A large phone, or a resizable browser at 700 pixels. | 1. Note the bottom tab bar.<br>2. Rotate to landscape, or widen past 768, and watch the bottom of the screen and the header.<br>3. Rotate back. | The tab bar disappears and the desktop navigation row appears, under the thumb, with nothing announced and no page state lost. | — |
| PHONE-14 | P2 | phone | The hamburger menu closes on the next route change but not on rotation, so it can be left open in a layout that no longer shows it ([Crossing the line while using the app](../cross-cutting/on-a-phone.md#crossing-the-line-while-using-the-app)). | A resizable browser at 390 pixels. | 1. Open the hamburger menu.<br>2. Widen past 768 without closing it, and read the header.<br>3. Narrow again and read the header. | The menu is hidden rather than closed while wide, and comes back open when the window narrows. A route change is what actually closes it. | — |
| PHONE-15 | P2 | phone | Live scoring is a single narrow column of large controls and stays a narrow column on a wide screen ([The one screen built for a phone](../cross-cutting/on-a-phone.md#the-one-screen-built-for-a-phone)). | A live match's page, at 390 pixels and then at 1440 pixels. | 1. Open the live page at 390 pixels and note the column and the padding at the foot.<br>2. Open it at 1440 pixels and note whether it spreads. | The same narrow column at both widths, centred on the wide screen rather than stretched. | — |
| PHONE-16 | P3 | phone | The hamburger button is 44 by 44 pixels and every live scoring control is at least 44 pixels tall; elsewhere the size is whatever the component library gives ([Touch and reach](../cross-cutting/on-a-phone.md#touch-and-reach)). | Browser at 390 x 844, element inspector open. | 1. Measure the hamburger button.<br>2. Measure three live scoring controls.<br>3. Measure three ordinary buttons elsewhere — a filter, a tab, a card action. | The first two meet 44 pixels. Record what the third group measures; the document says only the first two are set deliberately. | — |
| PHONE-17 | P2 | phone | The phone's safe areas are honoured in four places ([Touch and reach](../cross-cutting/on-a-phone.md#touch-and-reach)). | A real phone with a notch and a home indicator. | 1. Read the top bar against the notch.<br>2. Read the bottom tab bar against the home indicator.<br>3. Scroll a page to its foot.<br>4. Open `/playoffs` and read its own bottom bar. | Nothing sits under the notch or the home indicator in any of the four places. | — |
| PHONE-18 | P2 | phone | The bottom tab bar reaches four of the app's twenty routes; everything else is behind the hamburger or the user menu ([Touch and reach](../cross-cutting/on-a-phone.md#touch-and-reach)). | Browser at 390 x 844 with touch emulation. | 1. List the four tabs.<br>2. Open the hamburger and the user menu and list what each reaches.<br>3. Check that the message board, my team, history, insights, compare, help, contact and admin appear in one of the two. | The tab bar reaches four routes. Every other route is reachable only from the hamburger or the user menu, or by typing the address. | — |
| PHONE-19 | P2 | phone | The message board puts its list in a fixed-height scrolling box sized from the viewport, so the page has a scroller inside a scroller ([Touch and reach](../cross-cutting/on-a-phone.md#touch-and-reach)). | Browser at 390 x 844 with touch emulation, on `/message-board`, signed in. | 1. Drag the message list and note what moves.<br>2. Drag the page outside the list and note what moves. | The inner list scrolls. The outer page barely moves at all. | — |
| PHONE-20 | P1 | phone, visitor | The "Sign in to post messages" bar sits underneath the fixed tab bar and probably cannot be seen or tapped ([Edge cases](../cross-cutting/on-a-phone.md#edge-cases)). | A private window at 390 x 844 with touch emulation, on `/message-board`. | 1. Scroll to the very bottom of the page.<br>2. Look for the sign-in bar and note whether any of it is visible above the tab bar.<br>3. Try to tap it. | Record what happens. Expected: the bar is behind the tab bar, unreadable and untappable, so a signed-out visitor on a phone has no way to reach the sign-in prompt from the board. **(suspected bug)** | — |
| PHONE-21 | P1 | phone | A phone keyboard has no Escape, and the hamburger menu has no close-by-tapping-away either ([Cancel and interrupt](../cross-cutting/on-a-phone.md#cancel-and-interrupt)). | Browser at 390 x 844 with touch emulation. | 1. Open the hamburger menu and tap the page behind it.<br>2. Try to close it with a key.<br>3. Tap the button again. | Tapping away does not close it and no key reaches it. Only the button itself, or a route change, closes it. | — |
| PHONE-22 | P1 | phone | Scroll position is not reset on navigation, so a long page followed by a short one leaves a blank screen; only `/teams` restores ([Cancel and interrupt](../cross-cutting/on-a-phone.md#cancel-and-interrupt)). | Browser at 390 x 844 with touch emulation. | 1. Scroll `/schedule` a long way down.<br>2. Tap through to a short page and read what is on screen.<br>3. Open `/teams`, scroll down, open a team, and press back. | The short page opens scrolled down, showing blank screen. `/teams` returns to where it was, which no other route does. | — |
| PHONE-23 | P2 | phone | The back swipe is the browser's, behaves as an in-app navigation, and is the most likely way work is lost ([Cancel and interrupt](../cross-cutting/on-a-phone.md#cancel-and-interrupt)). | A real phone, a form part-filled. | 1. Type into any form.<br>2. Swipe back from the screen edge.<br>3. Swipe forward again and read the form. | You leave with no prompt and the typed text is gone. The app cannot intercept the gesture. | — |
| PHONE-24 | P2 | phone | A mobile browser discarding a background tab returns as a cold load, identical to a reload ([Cancel and interrupt](../cross-cutting/on-a-phone.md#cancel-and-interrupt)). | A real phone with several apps open and several browser tabs. | 1. Load several pages, then leave the browser for long enough that the tab is discarded.<br>2. Return and watch the app.<br>3. Check whether anything unsaved survived. | The whole app re-initialises, every page re-fetches, and nothing unsaved survives. It looks exactly like a reload. | — |
| PHONE-25 | P2 | phone, offline | Cached data stays on screen and looks current, the request fails, and nothing is queued ([Cancel and interrupt](../cross-cutting/on-a-phone.md#cancel-and-interrupt)). | A real phone on a live-scoring page, with aeroplane mode. | 1. Load the page, then turn aeroplane mode on.<br>2. Read the numbers on screen.<br>3. Attempt a write, then turn aeroplane mode off and wait five minutes. | The numbers stay and look current. The write fails and is lost — nothing is sent when the signal returns. | — |
| PHONE-26 | P3 | phone | A failure toast covers the top bar while it is there ([Interactions with other systems](../cross-cutting/on-a-phone.md#interactions-with-other-systems)). | Browser at 390 x 844, a write made to fail. | 1. Trigger a failure and watch the top of the screen.<br>2. Note what is hidden while the toast is up. | The top bar and the page heading are covered. The submit button at the foot is not. | — |
| PHONE-27 | P2 | phone, second scorer | Two phones at the same live match stay in step, while standings, records and the schedule do not ([Cancel and interrupt](../cross-cutting/on-a-phone.md#cancel-and-interrupt)). | Two phones, two accounts, both approved on opposite teams in the same **scratch match**. | 1. Enter a round on phone A and watch phone B without touching it.<br>2. Open `/stats` on both and have an admin change something that affects standings.<br>3. Watch both for two minutes. | The round arrives on B within seconds. The standings change reaches neither. Record `blocked` without a scratch match. | — |
| PHONE-28 | P2 | phone | Switching apps suspends the tab, and returning refetches anything past its fresh window, so numbers change as the app is re-opened ([Cancel and interrupt](../cross-cutting/on-a-phone.md#cancel-and-interrupt)). | A real phone on `/stats`. | 1. Note the numbers.<br>2. Switch to another app for six minutes.<br>3. Switch back and watch the numbers without touching anything. | The old numbers are shown, then update on their own. No spinner replaces them and nothing announces the change. | — |
| PHONE-29 | P2 | phone | The route into `/admin` is the user menu, which on a phone sits beside the hamburger ([Interactions with other systems](../cross-cutting/on-a-phone.md#interactions-with-other-systems)). | Admin, browser at 390 x 844 with touch emulation. | 1. Read the top bar and find the user menu.<br>2. Open it and find "Admin Panel".<br>3. Confirm the hamburger holds no admin entry. | The user menu is the only route in on a phone. The hamburger does not offer it. | — |
| PHONE-30 | P2 | phone | Mobile keyboards and password managers fill fields the same way as on a desktop, including the contact form's hidden bot trap ([Cancel and interrupt](../cross-cutting/on-a-phone.md#cancel-and-interrupt)). | A real phone with a password manager, on `/contact`. | 1. Invoke the manager's fill on the form.<br>2. Inspect whether the hidden trap field received a value.<br>3. Submit and read what the page says. | Ordinary fields fill normally. If the trap is filled, the submission is accepted and reported as sent while storing nothing; see LEAGUE-27. | — |
| PHONE-31 | P3 | phone | The winter theme adds two hundred animated snowflakes to the home page, at a battery and motion cost paid on a phone as well as a desktop ([Edge cases](../cross-cutting/on-a-phone.md#edge-cases)). | A real phone with the winter theme enabled by the league. | 1. Switch to the winter theme and open `/`.<br>2. Watch the page for a minute and note the animation.<br>3. Note whether a reduced-motion setting changes anything. | Continuous falling snow. A reduced-motion setting does not stop it; see A11Y-11. | — |
| PHONE-32 | P2 | phone | No page scrolls sideways on a narrow screen ([Open questions](../cross-cutting/on-a-phone.md#open-questions-and-verification)). | Browser at 390 x 844, and again at 320 pixels. | 1. Open each of the thirteen public routes at 390 pixels and try to drag each page sideways.<br>2. Repeat at 320 pixels.<br>3. Record any page that moves. | Record what happens. The app shell and every table wrapper are meant to prevent it, but no page has been measured. | — |
| PHONE-33 | P3 | phone | Whether the layout swap at 768 pixels is smooth or flashes when a phone is rotated is not settled ([Open questions](../cross-cutting/on-a-phone.md#open-questions-and-verification)). | A large phone that crosses 768 pixels in landscape. | 1. Rotate slowly and watch the header and the foot of the screen.<br>2. Rotate quickly and watch again.<br>3. Repeat five times. | Record whether the navigation swaps cleanly or flashes an intermediate state. | — |
| PHONE-34 | P2 | phone | Sharing a page from a phone shares the page, not the filter or the tab that was open ([Interactions with other systems](../cross-cutting/on-a-phone.md#interactions-with-other-systems)). | A real phone on `/schedule` with a filter applied. | 1. Apply a filter and read the address.<br>2. Share the page and read what is shared.<br>3. Open the shared address in a private window. | The address never carried the filter, so the shared link opens the unfiltered page. | — |

Not checkable by hand:

- The coarse device class recorded with every pageview — iOS, Android, other
  mobile, or desktop. Nothing about it is visible to the user; it is covered by
  [`what-the-league-sees.md`](../cross-cutting/what-the-league-sees.md).
- How the app behaves when installed to a home screen and opened with no
  connection. It needs an installed app and is the same question as ERR-13.
- The five named phases and the **Modifiers** table this document drops. The
  viewport is the modifier axis of every other document, and PHONE-01 to
  PHONE-14 already cover what a modifier table would have said; nothing is left
  over.

## cross-cutting/accessibility.md

Every claim in this document about what a screen reader *hears* is under "Not
checkable by hand" below: the device list in [`README.md`](README.md) has no
screen reader in it, and reading the accessibility tree in a browser inspector
answers "is the label there", never "was it announced". The items below are the
ones a `browser` or a `phone` can settle — focus, keys, roles and labels in the
tree, motion, and themes.

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A11Y-01 | P1 | browser | The first Tab reveals a "Skip to main content" link that is invisible until it is focused ([Keyboard](../cross-cutting/accessibility.md#keyboard)). | Any page, freshly loaded, focus on the body. | 1. Look for a skip link before pressing anything.<br>2. Press Tab once and read what is focused.<br>3. Press Enter and read where focus lands. | Nothing is visible before the Tab. The first Tab focuses "Skip to main content", and Enter moves focus into the main content. | — |
| A11Y-02 | P2 | browser | Every focusable control has a visible focus ring: two pixels, offset from the control, in the theme's ring colour ([The simple case](../cross-cutting/accessibility.md#the-simple-case)). | Any page, element inspector open. | 1. Tab through ten controls of different kinds — a link, a button, a select, a switch, a text field.<br>2. Measure the ring's width and offset on three of them.<br>3. Repeat in a different theme. | Every one shows a ring, two pixels wide and offset, in the theme's colour. None is focusable without a visible ring. | — |
| A11Y-03 | P1 | browser | An in-app navigation moves focus to the main content and writes the new page's name into a hidden live region, and the page does not scroll to the top ([The interaction, event by event](../cross-cutting/accessibility.md#the-interaction-event-by-event)). | Loaded `/`, inspector open on the live region, `document.activeElement` readable. | 1. Scroll down 400 pixels.<br>2. Click an in-app link.<br>3. Read the focused element, the live region's text, and the scroll position. | Focus is on the main content, the live region holds "*Page* page", and the scroll position has not changed — so what the region names and what is on screen can differ. | — |
| A11Y-04 | P1 | browser | The very first page load announces nothing and moves no focus, deliberately ([The interaction, event by event](../cross-cutting/accessibility.md#the-interaction-event-by-event)). | A cold load of `/` in a fresh tab. | 1. Load `/` directly.<br>2. Read the focused element.<br>3. Read the live region's text. | Focus is on the body, not the main content. The live region is empty. **Focus on the body at first load was confirmed by the automated pass on 2026-08-25.** | — |
| A11Y-05 | P1 | browser | Browser back or forward names the new page in the live region but does **not** move focus ([Screen readers](../cross-cutting/accessibility.md#screen-readers)). | Loaded `/`, navigated in-app to `/help`. | 1. Tab three times so focus sits somewhere identifiable.<br>2. Press the browser Back button.<br>3. Read the focused element and the live region. | The live region names the previous page. Focus is exactly where it was, not on the new page's main content. | — |
| A11Y-06 | P2 | browser | Every dialog, dropdown, popover, select and the bottom drawer traps focus, closes on Escape, and restores focus afterwards ([Keyboard](../cross-cutting/accessibility.md#keyboard)). | Any page holding each of the five. | 1. Open each in turn and press Tab repeatedly, noting whether focus leaves it.<br>2. Press Escape.<br>3. Read where focus lands. | Tab cycles inside each one. Escape closes each. Focus returns to whatever opened it, every time. | — |
| A11Y-07 | P1 | phone | The hamburger menu is not a dialog: it does not take focus, does not trap it, does not close on Escape, and does not express its open state ([Keyboard](../cross-cutting/accessibility.md#keyboard)). | Browser at 390 x 844, keyboard attached, accessibility tree open. | 1. Tab to the hamburger button and press Enter.<br>2. Read the focused element.<br>3. Press Tab repeatedly and note whether focus walks into the page behind.<br>4. Press Escape.<br>5. Read the button's node in the accessibility tree, open and closed. | Record what happens. Expected: focus stays on the button, Tab walks past the menu into the page, Escape does nothing, and the only difference in the tree is the button's name changing between "Open menu" and "Close menu" — no expanded state. **(suspected bug)** | — |
| A11Y-08 | P2 | browser | The only keyboard shortcut in the app is Cmd/Ctrl+K, it exists only from 768 pixels up, and it suppresses the browser's own use of that key ([Keyboard](../cross-cutting/accessibility.md#keyboard)). | A resizable browser on `/`. | 1. At 1024 pixels press Cmd/Ctrl+K and note what happens to the browser's own behaviour.<br>2. Try every other likely shortcut — `/`, `?`, `g` then a letter, Escape.<br>3. Narrow to 390 pixels and press Cmd/Ctrl+K. | The palette opens and the browser's own search does not. No other key combination does anything. Nothing happens below 768 pixels. | — |
| A11Y-09 | P2 | browser | Every spinner is a status region carrying its own message, while skeleton placeholders carry no status role at all ([Screen readers](../cross-cutting/accessibility.md#screen-readers)). | A throttled connection, accessibility tree open. | 1. Trigger "Loading page...", "Loading match..." and "Checking access..." and read each node in the tree.<br>2. Trigger a page that shows a grey skeleton and read its node. | The three spinners are status regions with text. The skeleton has no status role and no text, so whether a reader is told the page is loading depends on which placeholder the page uses. | — |
| A11Y-10 | P1 | browser | A page moving from a loading state to an empty state changes nothing in any live region ([Screen readers](../cross-cutting/accessibility.md#screen-readers)). | A page whose read returns nothing, or is made to fail, with the live regions visible in the inspector. | 1. Watch every live region on the page while it moves from loading to empty.<br>2. Read the empty state's node in the accessibility tree. | No live region changes. The empty state is ordinary content, so nothing distinguishes a slow list from an empty one. | — |
| A11Y-11 | P1 | browser | A reduced-motion setting reaches only the playoff bracket viewer's two transitions and nothing else in the product ([Motion](../cross-cutting/accessibility.md#motion)). | A browser with reduced motion emulated, and the winter theme enabled. | 1. Turn reduced motion on.<br>2. Load `/` and watch the route fade, the entrance animations, the skeleton shimmer, the spinner and the snowflakes.<br>3. Open `/playoffs`, open a bracket, and watch its transitions.<br>4. Turn reduced motion off and compare. | Record what happens. Expected: everything animates exactly as before except two transitions inside the bracket viewer. **(suspected bug)** | — |
| A11Y-12 | P2 | browser | The app defaults to dark and does not follow the operating system ([Contrast and themes](../cross-cutting/accessibility.md#contrast-and-themes)). | A device set to light mode, a fresh private window with no stored preference. | 1. Set the operating system to light mode.<br>2. Open the app in a fresh private window.<br>3. Note the theme, then press the theme button and reload. | The app opens dark despite the system setting. The pressed choice is remembered in the browser afterwards. | — |
| A11Y-13 | P2 | browser, admin | A theme the league turns off is swapped for dark under the user, silently, on their next visit ([Contrast and themes](../cross-cutting/accessibility.md#contrast-and-themes)). | A second browser signed in as a player using a theme the league can spare, plus an admin. | 1. As the player, choose that theme and confirm it sticks across a reload.<br>2. As the admin, disable it in the Themes section.<br>3. As the player, reload and note the theme and any message.<br>4. Re-enable the theme afterwards. | The player is moved to dark with nothing said. Record whether anything at all marks the change. | — |
| A11Y-14 | P2 | browser | Nothing in the product responds to a high-contrast or forced-colours setting ([Contrast and themes](../cross-cutting/accessibility.md#contrast-and-themes)). | A browser with forced colours emulated. | 1. Turn forced colours on.<br>2. Load `/`, `/stats` and `/playoffs`.<br>3. Compare against the same pages with it off. | The pages render identically. Nothing adapts. | — |
| A11Y-15 | P2 | browser | The theme button cycles between whichever themes the league has enabled, and the choice is remembered in the browser ([Contrast and themes](../cross-cutting/accessibility.md#contrast-and-themes)). | Any role, a browser with no stored preference. | 1. Press the theme button repeatedly and note the order and how many themes it cycles through.<br>2. Compare against the enabled list in the admin Themes section.<br>3. Reload and note the theme. | It cycles only the enabled themes, and the last one chosen survives a reload. | — |
| A11Y-16 | P2 | browser | Decorative icons are hidden from the accessibility tree and the unread dot on a notification is marked decorative ([Screen readers](../cross-cutting/accessibility.md#screen-readers)). | Any page with icon-only controls, accessibility tree open, plus the notification bell with unread items. | 1. Read five decorative icons' nodes in the tree.<br>2. Read a read and an unread notification's nodes and compare. | The icons are hidden. The two notification entries are identical in the tree — the dot carries nothing, so read and unread are indistinguishable there. | — |
| A11Y-17 | P2 | browser | Controls carry explicit labels, and some text exists only for the accessibility tree ([Screen readers](../cross-cutting/accessibility.md#screen-readers)). | Any page, accessibility tree open. | 1. Take a sample of twenty controls across four pages and read each one's accessible name.<br>2. Note any that have no name, or a name that is only an icon.<br>3. Find at least two nodes whose text is present in the tree and absent on screen. | Every sampled control has a real name. Record any that do not. The document's counts — 107 labelled controls and 14 reader-only strings — are code facts, not hand-checkable. | — |
| A11Y-18 | P2 | browser | Escape closes any dialog, menu, popover or select and returns focus, but does not close the hamburger menu ([Cancel and interrupt](../cross-cutting/accessibility.md#cancel-and-interrupt)). | A resizable browser, one of each control open in turn. | 1. Open a dialog, a dropdown menu, a popover and a select, pressing Escape on each.<br>2. Narrow to 390 pixels, open the hamburger menu, and press Escape. | The first four close and restore focus. The hamburger stays open — it is the one panel not built from those components. | — |
| A11Y-19 | P2 | browser | Field errors are tied to their fields and appear only after a failed submit, never as the user types ([Interactions with other systems](../cross-cutting/accessibility.md#interactions-with-other-systems)). | `/contact`, accessibility tree open. | 1. Type an invalid value and watch for a message while typing.<br>2. Submit and read the messages.<br>3. Read one field's node in the tree and confirm the message is tied to it. | Nothing appears while typing. After the submit each message is attached to its own field, so moving to the field reaches the message. | — |
| A11Y-20 | P2 | browser | The contact form's hidden bot trap is removed from tab order and hidden from the accessibility tree ([Cancel and interrupt](../cross-cutting/accessibility.md#cancel-and-interrupt)). | `/contact`, accessibility tree open. | 1. Tab through the whole form and note whether the trap is ever reached.<br>2. Find the trap in the DOM and read its attributes.<br>3. Look for it in the accessibility tree. | It is never reached by Tab, carries `tabindex="-1"` and `aria-hidden="true"`, and is absent from the tree. **Its attributes were confirmed by the automated pass on 2026-08-25.** | — |
| A11Y-21 | P2 | browser | A control a role may not use is absent rather than disabled, so it is equally invisible to everyone ([Interactions with other systems](../cross-cutting/accessibility.md#interactions-with-other-systems)). | A player, not an admin, accessibility tree open. | 1. Open a page whose admin controls are hidden.<br>2. Search the DOM and the accessibility tree for them. | They are not in the DOM at all — not disabled, not hidden. See PERM-08. | — |
| A11Y-22 | P2 | browser | Touch targets are set to 44 pixels deliberately only on live scoring and the hamburger ([Interactions with other systems](../cross-cutting/accessibility.md#interactions-with-other-systems)). | Browser at 390 x 844, element inspector open. | 1. Measure five live scoring controls and the hamburger.<br>2. Measure ten ordinary controls elsewhere in the app.<br>3. Record any below 44 pixels. | The first six meet it. Record how many of the other ten do not; the document says only the first group is set explicitly. | — |
| A11Y-23 | P3 | browser | Whether every page has exactly one first-level heading is not settled ([Open questions](../cross-cutting/accessibility.md#open-questions-and-verification)). | Any role, accessibility tree open. | 1. Open each of the thirteen public routes.<br>2. Count the level-one headings on each and note the landmark structure.<br>3. Record any page with none or more than one. | Record the count per page. The page shell was corrected once to avoid two main landmarks; the headings were never audited. | — |
| A11Y-24 | P3 | browser | Contrast ratios in the three themes are not settled, and the Lighthouse floor of 0.9 leaves room for failures on unscanned pages ([Open questions](../cross-cutting/accessibility.md#open-questions-and-verification)). | A browser with a contrast checker, all three themes available. | 1. In each theme, measure body text, muted text, a primary button, and a status badge against their backgrounds.<br>2. Record every ratio below 4.5 to 1.<br>3. Do the same on `/schedule` and a team page, which the scan does not cover. | Record the measured ratios. Nothing here has been checked before. | — |
| A11Y-25 | P2 | browser | Whether the app is usable at all with a reduced-motion setting and the winter theme on is not settled ([Open questions](../cross-cutting/accessibility.md#open-questions-and-verification)). | Winter theme enabled, reduced motion emulated, `/`. | 1. Turn both on and open `/`.<br>2. Try to read the page, use the navigation, and open a dialog, for two minutes.<br>3. Record what makes it hard. | Record what happens. The snowflakes are expected to keep falling; see A11Y-11. | — |

Not checkable by hand:

- **Every claim about what is announced.** The device list in
  [`README.md`](README.md) offers no screen reader, and a browser's
  accessibility tree shows what is *there*, not what is *heard*. That covers:
  whether the route announcement is heard in practice or arrives too early;
  whether toasts are announced and whether a replaced toast is re-announced;
  whether the shared red error bar is heard assertively; whether a live score, a
  new board message, or a new notification is announced when it arrives; whether
  a screen replacing itself — the contact form becoming a success panel, a
  game-won banner appearing — is announced; and whether data changing under the
  user is announced. All of these are described in
  [Screen readers](../cross-cutting/accessibility.md#screen-readers) and
  [Cancel and interrupt](../cross-cutting/accessibility.md#cancel-and-interrupt);
  they need a reader and a listener, and this checklist can only confirm what
  the accessibility tree contains.
- What the accessibility scan covers in continuous integration — six public
  routes, three admin tabs, no silenced rules, and a Lighthouse floor of 0.9.
  That is a fact about the repository, not about the running app; see
  [What is tested, and what is not](../cross-cutting/accessibility.md#what-is-tested-and-what-is-not).
- Whether the component library's dialogs, menus and selects behave accessibly
  out of the box. They are used unmodified and none was tested with a reader.
- The five named phases and the **Modifiers** table this document drops.
  Accessibility is a property of every interaction rather than one of its own,
  and its modifier axis is assistive technology, which is the subject of the
  bullet above.

## cross-cutting/what-the-league-sees.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| LEAGUE-01 | P1 | browser | Three things leave the browser on every route change: a Google Analytics pageview, a pageview to the league's own counter, and a route counter with a page-load timing ([What is recorded on every page](../cross-cutting/what-the-league-sees.md#what-is-recorded-on-every-page)). | The **published** app, network panel open, filter cleared. | 1. Open `/schedule` and watch the network panel from the first request.<br>2. Identify the three outbound calls and read what each carries.<br>3. Navigate to `/teams` and count them again. | Three calls per route change: a pageview carrying the path and the page title, a pageview carrying the path and a device class, and a route counter with a timing. Record `blocked` on a dev server. | — |
| LEAGUE-02 | P1 | browser | Nothing at all is recorded from a development build; analytics prints to the console instead ([Modifiers](../cross-cutting/what-the-league-sees.md#modifiers)). | The dev server, network panel and console both open. | 1. Navigate between four pages.<br>2. Read the network panel for any analytics, counter, or error-reporting request.<br>3. Read the console. | No outbound recording of any kind. The analytics call appears in the console rather than on the network. This is why most of this section is `blocked` on a dev server. | — |
| LEAGUE-03 | P2 | browser | None of the three carries the user's name, email address, or account ([The simple case](../cross-cutting/what-the-league-sees.md#the-simple-case)). | The **published** app, signed in, network panel open. | 1. Navigate once and read each of the three request payloads in full.<br>2. Search each for the signed-in name, the email address, and any account identifier. | None of the three carries any of them, signed in or out. Record `blocked` on a dev server. | — |
| LEAGUE-04 | P2 | browser | The league's own counter stores the path and a coarse device class only — no address and no user agent ([What is recorded on every page](../cross-cutting/what-the-league-sees.md#what-is-recorded-on-every-page)). | The **published** app, network panel open. | 1. Read the counter request's payload field by field.<br>2. Look for an address, a user agent, or anything more precise than iOS, Android, other mobile, desktop, or unknown. | Only the path and one of those five classes. Record `blocked` on a dev server. | — |
| LEAGUE-05 | P3 | browser | The counter accepts at most sixty pageviews a minute from one address and silently drops the rest ([What is recorded on every page](../cross-cutting/what-the-league-sees.md#what-is-recorded-on-every-page)). | The **published** app, an admin able to read the traffic table the next day. | 1. Navigate rapidly between pages more than sixty times inside a minute.<br>2. Watch the network panel for refusals.<br>3. Compare against the admin traffic table afterwards. | Fast browsing is undercounted rather than rejected — nothing on screen changes and nothing fails. Record `blocked` on a dev server. | — |
| LEAGUE-06 | P2 | admin | Only an admin can read any of it, as a per-day table of visitors, pageviews and the device split ([What is recorded on every page](../cross-cutting/what-the-league-sees.md#what-is-recorded-on-every-page)). | Admin, `/admin`, League Night section. | 1. Read the traffic area and name its columns.<br>2. Confirm no other role can reach it — `/admin` is guarded.<br>3. Look anywhere in the public app for the same numbers. | Visitors, pageviews, and counts by iOS, Android and other, per day. Nothing public shows any of it. | — |
| LEAGUE-07 | P3 | browser | Addresses in reported URLs have their tokens, keys, codes and email parameters replaced before sending ([What is recorded when something breaks](../cross-cutting/what-the-league-sees.md#what-is-recorded-when-something-breaks)). | The **published** app, network panel open, a URL carrying a token-like query parameter. | 1. Load a page whose address carries such a parameter.<br>2. Force an error that is not a plain network failure.<br>3. Read the reported payload's URL field. | The sensitive parameters are replaced rather than sent. Record `blocked` on a dev server. | — |
| LEAGUE-08 | P2 | offline | Plain network errors and page-code download failures are dropped on purpose, so a user on a bad connection generates no reports ([What is recorded when something breaks](../cross-cutting/what-the-league-sees.md#what-is-recorded-when-something-breaks)). | The **published** app, network panel open. | 1. Go offline and cause a dozen failed reads and writes.<br>2. Navigate to a page whose code has not downloaded.<br>3. Read the network panel for any error report leaving the browser. | Nothing is reported for either kind. Record `blocked` on a dev server. | — |
| LEAGUE-09 | P1 | browser | A session replay recorder is added twelve to fifteen seconds after the app loads, records about one session in ten and every session in which an error occurs, and nothing in the app mentions it ([What is recorded when something breaks](../cross-cutting/what-the-league-sees.md#what-is-recorded-when-something-breaks)). | The **published** app, network panel open, a stopwatch. | 1. Load the app and time how long before the recorder's script is requested.<br>2. Cause an error and watch what is sent afterwards.<br>3. Read the whole app — footer, help, contact, sign-in — for any mention of session recording or any way to opt out. | Twelve to fifteen seconds, then recording. Nothing anywhere tells the user, and there is no opt-out. Record `blocked` on a dev server. | — |
| LEAGUE-10 | P1 | browser | The contact form is the only email the product sends, and the sender's address is the reply-to so a reply lands in their inbox ([What a user writes, and where it goes](../cross-cutting/what-the-league-sees.md#what-a-user-writes-and-where-it-goes)). | `/contact`, an address you control, and access to the league's receiving inbox. | 1. Send a message from `/contact` with a distinctive subject.<br>2. Check the league's inbox for it and read its reply-to.<br>3. Reply and check whether it reaches the sender.<br>4. Repeat for a message board post, a score report, and a join request, checking that no email is sent for any of them. | One email, with the sender as reply-to. None of the other three produces an email of any kind. | — |
| LEAGUE-11 | P2 | player | Reporting a score tells nobody — not the other team and not an admin ([What a user writes, and where it goes](../cross-cutting/what-the-league-sees.md#what-a-user-writes-and-where-it-goes)). | A player, an admin in a second browser sitting on `/admin`, and a match missing a result. | 1. As the player, report a score.<br>2. Watch the admin's dashboard for two minutes without touching it.<br>3. Check the admin's bell and inbox.<br>4. Have the admin open the Pending section by hand. | Nothing reaches the admin on its own. The submission is only found by looking. The other team is not told at all. | — |
| LEAGUE-12 | P2 | player | Requesting to join a team tells nobody; an admin finds it by looking ([What a user writes, and where it goes](../cross-cutting/what-the-league-sees.md#what-a-user-writes-and-where-it-goes)). | A player with no membership, an admin in a second browser on `/admin`. | 1. As the player, request to join a team.<br>2. Watch the admin's Requests badge and bell for two minutes.<br>3. Open Teams then Member Approvals by hand. | The membership badge on the Teams tab is the only sign, and only once the tab is opened. Nothing is pushed anywhere. | — |
| LEAGUE-13 | P2 | player, admin | Being approved for a team tells nobody; the player finds out by reloading ([Edge cases](../cross-cutting/what-the-league-sees.md#edge-cases)). | A player waiting on `/my-team`, an admin in a second browser. | 1. As the admin, approve the membership.<br>2. Watch the player's screen for two minutes without touching it.<br>3. Check the player's bell and inbox.<br>4. Reload the player's page. | The player still reads "waiting for approval" until the reload. No email, no notification, and nothing on screen changes on its own. | — |
| LEAGUE-14 | P2 | player | Posting to the message board stores the username and the team name for every signed-in user to read, and tells nobody ([What a user writes, and where it goes](../cross-cutting/what-the-league-sees.md#what-a-user-writes-and-where-it-goes)). | A player with an approved membership, a second signed-in account. | 1. Post a message.<br>2. Read what is shown beside it.<br>3. As the second account, read the same message.<br>4. Check both accounts' inboxes and bells. | The username and the team name are stored on the message and shown to everybody signed in. Nobody is emailed or notified. | — |
| LEAGUE-15 | P1 | player | Finalising a live match moves standings, power scores and badges **after** the write, so a player looking immediately sees the match completed and the numbers not yet moved **(destructive)** ([What a user writes, and where it goes](../cross-cutting/what-the-league-sees.md#what-a-user-writes-and-where-it-goes)). | A **scratch match** ready to finalise, with `/stats` open in a second tab. | 1. Note both teams' records and power scores.<br>2. Finalise the match.<br>3. Reload `/stats` immediately and read the numbers.<br>4. Reload again after two minutes. | The match reads completed at once; the numbers move some time afterwards. Record how long the gap is. Record `blocked` without a scratch match. | — |
| LEAGUE-16 | P1 | visitor | An in-app notification appears in the bell for everybody, signed-out visitors included, without a reload ([Notifications](../cross-cutting/what-the-league-sees.md#notifications)). | An admin in one browser, a private window signed out with the app open. | 1. Leave the private window open on `/schedule`.<br>2. As the admin, post a test notification.<br>3. Watch the private window's bell for two minutes without touching it.<br>4. Delete the test notification afterwards. | The badge appears in the signed-out browser with no reload. This is one of the few things in the product that arrives on its own. | — |
| LEAGUE-17 | P1 | browser, second browser | Whether a notice has been seen is stored in the browser and nowhere else: per browser, per device, shared between tabs, and never reaching the league ([Notifications](../cross-cutting/what-the-league-sees.md#notifications)). | Two different browsers on the same account, a second tab in the first, and an admin who can read the league's own records. | 1. Open the bell in browser A, tab 1, and read the badge in tab 2.<br>2. Read the badge in browser B.<br>3. As the admin, look for any record of who has read what.<br>4. Clear site data in browser A and read the badge again. | Cleared in both tabs of A, still counting in B, no record anywhere on the league's side, and back to counting in A after the data is cleared. | — |
| LEAGUE-18 | P2 | browser | Push notifications do not come from the product at all, and nothing a player does can trigger one ([Notifications](../cross-cutting/what-the-league-sees.md#notifications)). | A phone with the app installed and push allowed, plus the admin screens. | 1. Read `/admin` and `/admin/notifications` for any control that sends a push.<br>2. Post an in-app notification and watch the phone with the app closed for ten minutes.<br>3. Finalise a match, post to the board, and report a score, watching the phone after each. | No control anywhere sends a push, and none of the four actions produces one. Anything that does arrive came from the third-party service's own dashboard. | — |
| LEAGUE-19 | P2 | browser | Three third-party addresses are contacted: the installable-app and push service on every page, Google Analytics in the published build, and a bracket-drawing script only on playoff pages ([What the browser fetches from other people](../cross-cutting/what-the-league-sees.md#what-the-browser-fetches-from-other-people)). | The **published** app, network panel open, filtered to third-party hosts. | 1. Load `/` and list every third-party host contacted.<br>2. Navigate to `/schedule` and list them again.<br>3. Open `/playoffs` and list them again. | The app and push service on every page; analytics shortly after each load; and the bracket script only once a playoff page is opened. Record `blocked` on a dev server for the analytics row. | — |
| LEAGUE-20 | P2 | offline | Every recorded event is fire-and-forget: a failure is swallowed silently and the league simply has one fewer number ([The life of one recorded event](../cross-cutting/what-the-league-sees.md#the-life-of-one-recorded-event)). | The **published** app, network panel open, the analytics and counter hosts blocked. | 1. Block the recording hosts and navigate between four pages.<br>2. Watch the screen for any error, toast, or retry.<br>3. Watch the network panel for a retry. | Nothing on screen and no retry. The user sees nothing at all. Record `blocked` on a dev server. | — |
| LEAGUE-21 | P2 | browser | The new route is recorded immediately, before the page has drawn ([Cancel and interrupt](../cross-cutting/what-the-league-sees.md#cancel-and-interrupt)). | The **published** app, network panel open, connection throttled. | 1. Navigate to a heavy page.<br>2. Note whether the pageview request leaves before or after the page's content appears. | The pageview leaves first, while the page is still loading. Record `blocked` on a dev server. | — |
| LEAGUE-22 | P2 | browser | Two rapid navigations to the same path within half a second are counted once ([Cancel and interrupt](../cross-cutting/what-the-league-sees.md#cancel-and-interrupt)). | The **published** app, network panel open. | 1. Click the same in-app link twice within half a second.<br>2. Count the pageview requests.<br>3. Repeat with a one-second gap and count again. | One request for the rapid pair, two for the slower pair. Record `blocked` on a dev server. | — |
| LEAGUE-23 | P2 | browser | Back and forward are recorded as fresh pageviews, so bouncing between two pages inflates both counts ([Cancel and interrupt](../cross-cutting/what-the-league-sees.md#cancel-and-interrupt)). | The **published** app, network panel open. | 1. Navigate from `/` to `/teams`.<br>2. Press Back and Forward five times each, slowly.<br>3. Count the pageview requests. | Every press produces another pageview. Nothing distinguishes a back from a fresh visit. Record `blocked` on a dev server. | — |
| LEAGUE-24 | P2 | browser | The pageview survives a tab close, because it is sent in a way the browser finishes after the page is gone ([Cancel and interrupt](../cross-cutting/what-the-league-sees.md#cancel-and-interrupt)). | The **published** app, network panel open with "preserve log" on. | 1. Navigate to a new page and immediately close the tab.<br>2. Read the preserved log for the pageview.<br>3. Compare against the admin traffic table the next day if possible. | The pageview is completed by the browser after the page is gone. Record `blocked` on a dev server. | — |
| LEAGUE-25 | P2 | offline | Nothing is recorded and nothing is queued while offline: the league sees a gap, not an error ([Cancel and interrupt](../cross-cutting/what-the-league-sees.md#cancel-and-interrupt)). | The **published** app, offline, network panel open. | 1. Go offline and navigate between four already-loaded pages.<br>2. Come back online and wait five minutes without navigating.<br>3. Watch the network panel throughout. | No recording while offline and nothing sent afterwards to make up for it. Record `blocked` on a dev server. | — |
| LEAGUE-26 | P2 | browser | Pageviews carry no session, so an expired session does not change what is recorded ([Cancel and interrupt](../cross-cutting/what-the-league-sees.md#cancel-and-interrupt)). | The **published** app, signed in, then with the session expired. | 1. Record a pageview's payload while signed in.<br>2. Expire the session and record another payload.<br>3. Compare them field by field. | Identical. Nothing about the session is in either. Record `blocked` on a dev server. | — |
| LEAGUE-27 | P1 | browser | The contact form's hidden bot trap accepts a submission, stores it nowhere, and tells the user it was sent ([Cancel and interrupt](../cross-cutting/what-the-league-sees.md#cancel-and-interrupt)). | `/contact`, the DOM inspector, and access to the league's receiving inbox and the support tickets. | 1. Find the hidden trap field and put a value in it by hand.<br>2. Fill the visible fields with a distinctive message and submit.<br>3. Read what the page says.<br>4. Check the league's inbox and the stored tickets for the message. | The page reports success. Nothing arrives anywhere, so the sender waits for a reply to a message the league never received. | — |
| LEAGUE-28 | P2 | player | Renaming a team changes it everywhere the team appears, at once, and tells nobody ([What a user writes, and where it goes](../cross-cutting/what-the-league-sees.md#what-a-user-writes-and-where-it-goes)). | A member of a **scratch team**. | 1. Note the team's name on `/stats`, `/schedule`, its own page and in history.<br>2. Rename it from the team page.<br>3. Reload all four and check every place the old name appeared.<br>4. Check whether anyone is told. | The new name is everywhere at once. Nobody is notified. Record `blocked` without a scratch team. | — |
| LEAGUE-29 | P2 | browser, second browser | The unread badge on the bell is per device, so reading a notice on a laptop leaves it unread on a phone ([Edge cases](../cross-cutting/what-the-league-sees.md#edge-cases)). | An admin who can post, a laptop and a phone signed in as the same account. | 1. Post a notification.<br>2. Open the bell on the laptop.<br>3. Read the badge on the phone without touching it.<br>4. Delete the test notification afterwards. | Cleared on the laptop, still counting on the phone, for the same person and the same account. | — |
| LEAGUE-30 | P2 | browser | The notification bell holds an open channel on every page of the app ([Interactions with other systems](../cross-cutting/what-the-league-sees.md#interactions-with-other-systems)). | Any role, network panel filtered to websockets. | 1. Load `/` and note the open connection.<br>2. Navigate to four other routes, including ones with no realtime feature of their own.<br>3. Confirm the connection is open on each. | The channel is open on every page, which makes it the only always-on realtime connection in the product. | — |
| LEAGUE-31 | P2 | browser | The recorded path is the address only, so no filter, tab, or sort order is ever counted ([Interactions with other systems](../cross-cutting/what-the-league-sees.md#interactions-with-other-systems)). | The **published** app, network panel open, on `/schedule`. | 1. Apply a filter, change a tab, and change a sort order.<br>2. Read the address bar after each.<br>3. Read the recorded path in the pageview payload. | The address never changes, so the recorded path is the same however the page is filtered. Record `blocked` on a dev server. | — |
| LEAGUE-32 | P3 | browser | No accessibility choice is recorded or transmitted ([Interactions with other systems](../cross-cutting/what-the-league-sees.md#interactions-with-other-systems)). | The **published** app, network panel open, reduced motion emulated and a theme chosen. | 1. Set reduced motion, choose a theme, and navigate.<br>2. Read every outbound payload for a theme, a motion preference, or a contrast setting. | None of the three appears in anything that leaves the browser. Record `blocked` on a dev server. | — |
| LEAGUE-33 | P2 | browser | There is no session-length or dwell-time measurement, and nothing new is recorded when the window loses focus ([Cancel and interrupt](../cross-cutting/what-the-league-sees.md#cancel-and-interrupt)). | The **published** app, network panel open. | 1. Sit on one page for five minutes and watch the network panel.<br>2. Switch away for five minutes and return, watching again. | Nothing is recorded for the time spent, for leaving, or for returning. Record `blocked` on a dev server. | — |
| LEAGUE-34 | P3 | browser | The league's counter refuses a path longer than 256 characters and an unknown device class, silently ([Interactions with other systems](../cross-cutting/what-the-league-sees.md#interactions-with-other-systems)). | The **published** app, a nonsense route longer than 256 characters. | 1. Type a path longer than 256 characters.<br>2. Read the counter request's response in the network panel.<br>3. Watch the screen for any message. | The request is refused and nothing at all is shown to the user — the Page Not Found screen behaves normally. Record `blocked` on a dev server. | — |
| LEAGUE-35 | P3 | browser | A user is counted as two visitors by browsing across midnight UTC, and as one visitor with somebody who shares an address and a browser ([Edge cases](../cross-cutting/what-the-league-sees.md#edge-cases)). | The **published** app, an admin able to read the traffic table, two people on one network and browser build. | 1. Browse a few pages either side of midnight UTC from one device.<br>2. Have two people on the same address and browser browse the same day.<br>3. Read the admin traffic table the next day and compare the visitor counts. | The single user appears as two visitors across the boundary; the two people appear as one. Record `blocked` on a dev server or without access to the table. | — |

Not checkable by hand:

- Whether registering actually sends a confirmation email in the published app.
  The local configuration switches confirmations off and the published setting
  was never read.
- What a push notification looks like when it arrives, who receives it, and
  whether a player can turn it off. That lives entirely in a third-party
  dashboard and is not visible from the product; see
  [Notifications](../cross-cutting/what-the-league-sees.md#notifications).
- Whether the traffic table in the admin dashboard matches the Google Analytics
  numbers, or by how much they differ. Needs the analytics account as well as
  the dashboard.
- The exact wording and content of the support email as received. Needs the
  league's receiving inbox rather than the app.
- Whether any database trigger calls out to anything. Needs the migrations, not
  the UI.
- The five named phases this document drops. Recording a pageview is not a form
  and has no editing phase; the life of one recorded event covers the same
  ground and LEAGUE-20 to LEAGUE-26 check it.
