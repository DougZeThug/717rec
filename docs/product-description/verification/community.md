# Verification: community

How to run this file: the message board needs a **signed-in player** for almost
everything and a **second browser** signed in as a *different* account for the
live items — a second tab shares the session and will mask what two real users
would see. Have an **admin** to hand for the Announcement category and for the
signed-out comparisons, and a **private window** for every `visitor` item. The
help page needs nothing. **The contact form is the pilot for this directory and
two of its claims are already answered** — see the Result column below, and read
the warning above the `contact-the-league.md` table before running anything
there. Every message posted and every reaction added during a pass is a **real
write to the real league's board**, visible to everyone: post obvious test text,
delete it afterwards, and do not react to other people's messages. Device values:
`visitor`, `player`, `admin`, `browser`, `phone`, `offline`, `slow`,
`second browser`.

## message-board/read-the-board.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BOARD-01 | P1 | player | **There are no threads.** A message cannot be attached to another, there is no reply, and nothing is nested ([Summary](../message-board/read-the-board.md#summary)). | Signed in, a board with several messages. | 1. Press and long-press somebody else's message.<br>2. Read every control that appears.<br>3. Look for any indentation or "in reply to" line anywhere in the list. | No reply control anywhere. Every message sits at the same level. The only grouping offered is a filter. | — |
| BOARD-02 | P2 | player | A sticky bar at the top holds the title, a search box, a refresh button and a filter button ([The simple case](../message-board/read-the-board.md#the-simple-case)). | Signed in on `/message-board`. | 1. Read the bar.<br>2. Scroll the list and watch the bar. | All four present, and the bar stays at the top as the list scrolls. | — |
| BOARD-03 | P2 | player | Each message shows who wrote it, their team as a small coloured badge, the time, and the text with its line breaks kept ([The simple case](../message-board/read-the-board.md#the-simple-case)). | A multi-line message on the board. | 1. Read one message top to bottom.<br>2. Compare its line breaks with how it was typed. | Author, team badge, time, then the text exactly as typed. | — |
| BOARD-04 | P2 | player | Ten messages load at a time, and scrolling past the tenth shows "Loading more messages..." and brings ten more ([The simple case](../message-board/read-the-board.md#the-simple-case)). | A board with more than twenty messages. | 1. Count the messages on arrival.<br>2. Scroll to the bottom of the list.<br>3. Read the line that appears and count again. | Ten, then that exact sentence, then twenty. | — |
| BOARD-05 | P3 | slow | While the first page is in flight the card shows five grey message-shaped placeholders ([Arrive](../message-board/read-the-board.md#arrive)). | Throttled, fresh session. | 1. Open `/message-board`.<br>2. Count the grey shapes. | Five, shaped like messages rather than a spinner. | — |
| BOARD-06 | P1 | visitor | **A signed-out visitor gets an empty board and is told to be the first to start a conversation** ([Edge cases](../message-board/read-the-board.md#edge-cases)). **(suspected bug)** | Private window, signed out, a board that is not actually empty. | 1. Open `/message-board`.<br>2. Read the whole card and the bar at the foot. | Record what happens. The expectation is the crossed message icon, "No Messages Yet", "Be the first to start a conversation!", and a fixed "Sign in to post messages" bar — an invitation the visitor cannot act on, with nothing saying the board is hidden rather than empty. The automated pass on 2026-08-25 confirmed the sign-in bar and button; the empty state itself is what this item adds. | — |
| BOARD-07 | P2 | visitor | The Sign In button goes to the sign-in page and returns here afterwards ([Arrive](../message-board/read-the-board.md#arrive)). | Signed out on `/message-board`, an account to hand. | 1. Press Sign In.<br>2. Sign in.<br>3. Read where you land. | Back on `/message-board`, with the messages now readable and the composer in place of the bar. | — |
| BOARD-08 | P1 | offline | A refused first page turns the card into a red panel reading "Failed to load messages" and "Please try refreshing the page", **with no retry button** ([Arrive](../message-board/read-the-board.md#arrive)). | Network blocked, signed in. | 1. Block the network.<br>2. Open `/message-board`.<br>3. Read the panel and look for a retry. | Both sentences, in red, and no retry control of any kind. | — |
| BOARD-09 | P1 | player | **Filters survive from the reader's last visit and the panel that lists them starts closed** ([Arrive](../message-board/read-the-board.md#arrive)). **(suspected bug)** | Signed in. | 1. Filter the board to one team.<br>2. Navigate to `/help`, then back to `/message-board`.<br>3. Read the board and the filter button without opening the panel. | Record what happens. The expectation is a filtered list with the panel closed, and a tinted filter button carrying a count as the only clue. | — |
| BOARD-10 | P2 | player | Nothing is focused on arrival ([Arrive](../message-board/read-the-board.md#arrive)). | Fresh load. | 1. Load the board.<br>2. Without clicking, type "abc". | Nothing appears in the search box or the composer. Focus is on the body. | — |
| BOARD-11 | P2 | player | Coming back re-fetches the first ten messages every time; the board is never served from cache without checking ([Leave without changing anything](../message-board/read-the-board.md#leave-without-changing-anything)). | Network panel open. | 1. Load the board and scroll to load thirty messages.<br>2. Navigate to `/help` and back within one minute.<br>3. Count the messages and watch the network. | A fresh request, and ten messages again rather than thirty. The scroll position and the loaded pages are discarded. | — |
| BOARD-12 | P1 | player | **Typing in the search box does not filter anything by itself**; the search runs only on Enter or the magnifying glass, and it runs on the league's side ([Begin editing](../message-board/read-the-board.md#begin-editing)). | Network panel open, signed in. | 1. Type a word that appears in only one message.<br>2. Wait five seconds and watch the list and the network.<br>3. Press Enter. | Nothing changes and no request is made while typing. Enter fetches the whole list again, matched on the message text. | — |
| BOARD-13 | P2 | player | Choosing a category or a team from the filter panel applies immediately and fetches again ([Begin editing](../message-board/read-the-board.md#begin-editing)). | Filter panel open, network panel open. | 1. Choose a team.<br>2. Watch the list and the network. | A request goes out at once and the list resets to the first ten matches. No confirm step. | — |
| BOARD-14 | P1 | player | Every filter change resets the list to the first ten matches, and **the scroll position is not reset with it** ([While editing](../message-board/read-the-board.md#while-editing)). | A board long enough to scroll far down. | 1. Scroll a long way down the list.<br>2. Apply a filter that matches only two messages.<br>3. Read the screen without scrolling. | Record what happens. The expectation is that the reader is left looking at nothing, because the list shrank under them and the scroll offset did not move. | — |
| BOARD-15 | P2 | player | Filters combine, each appears as a chip with an X, and "Clear filters" removes all three and closes the panel ([While editing](../message-board/read-the-board.md#while-editing)). | Signed in. | 1. Set a category, a team and a search term.<br>2. Read the chips in the panel.<br>3. Press "Clear filters". | Three chips, each removable on its own. Clear removes all three and closes the panel. | — |
| BOARD-16 | P1 | visitor | **The refresh button reports success when it loaded nothing** ([Edge cases](../message-board/read-the-board.md#edge-cases)). **(suspected bug)** | Signed out on `/message-board`. | 1. Press refresh.<br>2. Read the toast and the list. | Record what happens. The expectation is "Messages refreshed — Latest messages have been loaded" over an empty board for a visitor who cannot read messages at all. | — |
| BOARD-17 | P2 | player | The URL never changes: a filtered board cannot be linked to or bookmarked ([While editing](../message-board/read-the-board.md#while-editing)). | Signed in. | 1. Set every filter and run a search.<br>2. Read the address bar. | `/message-board` with no query string throughout. | — |
| BOARD-18 | P1 | player | Pressing a reaction chip changes the count **immediately**, before the league has answered, and tints the chip while the reaction is the reader's own ([Submit](../message-board/read-the-board.md#submit)). | Throttled connection, a test message of the tester's own with an existing chip. | 1. Press a chip and watch the count in the same instant.<br>2. Press it again. | The count moves before the request finishes and the chip is tinted. Pressing again removes the reaction and un-tints it. | — |
| BOARD-19 | P2 | offline | A refused reaction puts the count back and shows a red toast ([Submit](../message-board/read-the-board.md#submit)). | Board loaded, then offline. | 1. Go offline.<br>2. Press a chip.<br>3. Read the count and the toast. | The count returns to what it was. "Error — Failed to add reaction", or "Failed to remove reaction" for the other direction. | — |
| BOARD-20 | P2 | visitor | A reader who is not signed in gets "Sign in required — Please sign in to react to messages" and nothing is sent ([Submit](../message-board/read-the-board.md#submit)). | Signed out, network panel open, a board with a visible chip if one can be reached. | 1. Press a chip.<br>2. Read the toast and watch the network. | That plain toast, and no request. | — |
| BOARD-21 | P1 | player, second browser | **The board is live**: a message posted by someone else appears at the top without the reader acting ([Cancel and interrupt](../message-board/read-the-board.md#cancel-and-interrupt)). | Two browsers, two different accounts, both on `/message-board`. | 1. In browser B, post a test message.<br>2. Watch browser A without touching it for 30 s. | The message drops in at the top of browser A's list. No refresh, no toast. | — |
| BOARD-22 | P2 | player, second browser | An edit rewrites a message in place and a delete removes it, both live ([Cancel and interrupt](../message-board/read-the-board.md#cancel-and-interrupt)). | As BOARD-21, with a test message from browser B on screen in A. | 1. Edit the message in browser B.<br>2. Watch browser A.<br>3. Delete it in browser B and watch again. | The text changes under the reader and gains "(edited)". The delete removes it from browser A's list. | — |
| BOARD-23 | P2 | player, second browser | **A message that does not match the current filters is not added** ([Cancel and interrupt](../message-board/read-the-board.md#cancel-and-interrupt)). | Browser A filtered to one team, browser B on a different team. | 1. Post from browser B.<br>2. Watch browser A for 30 s.<br>3. Clear the filter in browser A. | The message does not arrive while the filter is set. It appears once the filter is cleared. | — |
| BOARD-24 | P2 | player, second browser | Reactions from other people arrive while the page is open, so a count can go up under the reader ([Submit](../message-board/read-the-board.md#submit)). | Two browsers on the same message. | 1. React from browser B.<br>2. Watch the chip in browser A without touching it. | The count rises in browser A on its own. Record how long it takes. | — |
| BOARD-25 | P1 | player | **To use an emoji nobody has used yet the reader must press and hold for about half a second, and there is no keyboard equivalent** ([Edge cases](../message-board/read-the-board.md#edge-cases)). **(suspected bug)** | Signed in, somebody else's message with no reactions. | 1. Look for any visible control that adds a reaction.<br>2. Press and hold the message for half a second and count the emoji in the panel.<br>3. Tab to the message and try every key to open the same panel. | Record what happens. The expectation is nothing on screen suggesting the gesture, twenty-four emoji in the panel, and no key that opens it. | — |
| BOARD-26 | P2 | player | The reaction panel has its own X button and **Escape does not close it** ([Cancel and interrupt](../message-board/read-the-board.md#cancel-and-interrupt)). | Reaction panel open. | 1. Press Escape.<br>2. Press the X. | Escape leaves the panel open. Only the X closes it. | — |
| BOARD-27 | P2 | player | Escape in the search box clears the box but does not undo a search already run ([Cancel and interrupt](../message-board/read-the-board.md#cancel-and-interrupt)). | A search run with Enter. | 1. Run a search.<br>2. Press Escape in the search box.<br>3. Read the list. | The box empties. The list is still the search result until the search is run again on the empty box. | — |
| BOARD-28 | P2 | player | A reaction already sent still lands after navigating away, and its failure toast can appear over an unrelated page ([Cancel and interrupt](../message-board/read-the-board.md#cancel-and-interrupt)). | Throttled connection. | 1. Press a chip and immediately click a link to `/help`.<br>2. Watch `/help` for five seconds.<br>3. Return to the board and read the chip. | The result is never seen on the board. A failure toast, if any, appears over `/help`. The reaction is applied. | — |
| BOARD-29 | P2 | offline | A "load more" failure keeps the messages already on screen and shows its own message ([Cancel and interrupt](../message-board/read-the-board.md#cancel-and-interrupt)). | Board loaded, then offline. | 1. Load the board.<br>2. Go offline.<br>3. Scroll to the bottom. | "Error loading messages — Could not load additional messages. Please try again." The ten already loaded stay. Nothing is queued. | — |
| BOARD-30 | P2 | offline | A failed refresh shows "Refresh failed — Could not refresh messages. Please try again." ([Cancel and interrupt](../message-board/read-the-board.md#cancel-and-interrupt)). | Board loaded, then offline. | 1. Press refresh while offline.<br>2. Read the toast. | That exact sentence, and the list unchanged. | — |
| BOARD-31 | P1 | player | **An expired session makes the board silently empty**, exactly like being signed out, with nothing to say why ([Cancel and interrupt](../message-board/read-the-board.md#cancel-and-interrupt)). | A session made to expire while the board is open. | 1. Let the session expire, or sign out from a browser sharing it.<br>2. Press refresh on the board. | Record what happens. The expectation is an empty board with the same "No Messages Yet" state and no mention of the session. | — |
| BOARD-32 | P2 | player, second browser | If the live connection dropped while the tab was away it is rebuilt and the first page is fetched again on reconnecting ([Cancel and interrupt](../message-board/read-the-board.md#cancel-and-interrupt)). | Browser A on the board, then taken offline and backgrounded. | 1. Background browser A and take it offline for two minutes.<br>2. Post from browser B.<br>3. Bring A online and return to the tab. | The missed message is there after the reconnection, because the first page is fetched again. Nothing was lost. | — |
| BOARD-33 | P1 | player | **Every message shows a clock time and no date, and there is no day separator anywhere in the list** ([Edge cases](../message-board/read-the-board.md#edge-cases)). **(suspected bug)** | A board with messages from several different days. | 1. Read the time on the newest message and on one from a previous week.<br>2. Scroll the whole list looking for a date or a separator. | Record what happens. The expectation is that both read like "3:42 PM" and nothing anywhere marks the change of day. | — |
| BOARD-34 | P1 | player | **Three of the five category filters can never match anything** ([Edge cases](../message-board/read-the-board.md#edge-cases)). **(suspected bug)** | Signed in, a populated board. | 1. Open the filter panel and read all five categories.<br>2. Choose Question, then Event, then Other, reading the list each time.<br>3. Choose General, then Announcement. | Record what happens for all five. The expectation is that Question, Event and Other always empty the board, because the composer can only produce General and — for an admin — Announcement. | — |
| BOARD-35 | P2 | player | An announcement gets a blue border and an "Announcement" badge; an edited message carries "(edited)" with the time in a tooltip; a message with no reactions shows no reaction row at all ([Modifiers](../message-board/read-the-board.md#modifiers)). | An admin announcement, an edited message, and a message with no reactions. | 1. Read all three. | Blue border and badge; "(edited)" with a tooltip giving how long ago; no reaction row rather than an empty one. | — |
| BOARD-36 | P2 | player, second browser | Signing in in another tab does not reach this page; the board stays empty until it is reloaded ([Modifiers](../message-board/read-the-board.md#modifiers)). | Browser A signed out on `/message-board`. | 1. Sign in from browser B, or from another tab of A.<br>2. Watch A's board for two minutes. | The board stays empty. Only a reload fills it. | — |
| BOARD-37 | P2 | player | The board belongs to no season and is never cleared between them ([Modifiers](../message-board/read-the-board.md#modifiers)). | A league that has changed season at least once. | 1. Scroll back far enough to reach a message from before the changeover. | It is still there, in the same flat list, with nothing marking the boundary. | — |
| BOARD-38 | P2 | phone | On a phone the list is a fixed-height panel inside the page, so the page and the list scroll separately ([Interactions with other systems](../message-board/read-the-board.md#interactions-with-other-systems)). | Phone, or 390 × 844 with touch emulation. | 1. Scroll to the bottom of the message list.<br>2. Keep scrolling in the same gesture. | Record whether reaching the bottom of one continues into the other. The composer or sign-in bar stays fixed above the bottom navigation throughout. | — |
| BOARD-39 | P2 | player | **The live list is capped at a hundred messages**: a new arrival pushes the oldest out, and scrolling back down fetches it again ([Edge cases](../message-board/read-the-board.md#edge-cases)). | A board with well over a hundred messages, and a second account to post from. | 1. Scroll until a hundred messages are loaded.<br>2. Post from browser B.<br>3. Scroll to the bottom of the list. | The oldest goes as the new one arrives. Scrolling down fetches it back. | — |
| BOARD-40 | P2 | player | **The team badge is coloured by that team's current power score**, not by anything about the message ([Edge cases](../message-board/read-the-board.md#edge-cases)). | A message weeks old from a team whose power score has changed band. | 1. Note the badge colour on an old message.<br>2. Read the team's current Power band on `/stats`. | The colour matches the team's power score **today**, not what it was when the message was posted. | — |
| BOARD-41 | P2 | player | **Match comments are a different feature** and never appear here ([Edge cases](../message-board/read-the-board.md#edge-cases)). | A match with comments on `/schedule`. | 1. Read the comments on the match card.<br>2. Search the board for the same text. | Nothing. The two are stored separately; see [`schedule/a-match-card.md`](../schedule/a-match-card.md). | — |

Not checkable by hand:

- How many open channels the board really holds with a full list on screen — the
  document says one for messages plus one per visible message — and whether that
  is a problem in practice. Needs instrumentation, not a look at the page. See
  [Open questions](../message-board/read-the-board.md#open-questions-and-verification).
- Whether the board pages by time rather than by position, and whether two
  messages stored in the same instant can straddle the boundary and one be
  skipped. Producing that collision needs the database.
- Whether the glossary or the database is right about a visitor reading the board.
  BOARD-06 gathers the evidence; which document to correct is a decision for the
  consistency pass.
- Whether the list's live region is announced by a screen reader when messages
  arrive. Needs a real screen reader; see
  [`cross-cutting/accessibility.md`](../cross-cutting/accessibility.md).

## message-board/post-and-reply.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| POST-01 | P1 | player | **There is no reply.** A message cannot be attached to another, quoted, or addressed to anyone ([Summary](../message-board/post-and-reply.md#summary)). | Signed in on `/message-board`. | 1. Read every control on somebody else's message.<br>2. Read every control on the composer. | No reply, no quote, no "to:" field. The only way to answer is a new message that lands at the top like any other. | — |
| POST-02 | P1 | admin | **An admin cannot remove another person's message**, so the board has no moderation at all ([Summary](../message-board/post-and-reply.md#summary)). | Signed in as admin, somebody else's message on the board. | 1. Click and long-press that message.<br>2. Search the admin dashboard for any message control. | No pencil, no bin, nothing in the dashboard. Admin adds the Announcement category and takes nothing away. | — |
| POST-03 | P2 | player | The composer is one box reading "Type a message...", a round send button inside it, and "500 left" underneath on the right ([The simple case](../message-board/post-and-reply.md#the-simple-case)). | Signed in. | 1. Scroll to the foot of the board and read the composer. | That placeholder, that button, that counter. | — |
| POST-04 | P2 | player | The box starts empty and **is not focused**, and the send button is live from the start ([Arrive](../message-board/post-and-reply.md#arrive)). | Fresh load of `/message-board`. | 1. Without clicking, type "abc".<br>2. Read the box and the send button. | Nothing was typed into the box. The button is enabled with nothing in the box. | — |
| POST-05 | P2 | admin | An admin sees a category picker with General and Announcement, starting on General; a player has none ([Arrive](../message-board/post-and-reply.md#arrive)). | Signed in as admin, then as a player. | 1. Read beside the counter as an admin.<br>2. Read the same place as a player. | Two choices for the admin, starting on General. No picker at all for the player. | — |
| POST-06 | P2 | player | Nothing is drafted, autosaved, or recorded by arriving ([Leave without changing anything](../message-board/post-and-reply.md#leave-without-changing-anything)). | Signed in. | 1. Type half a message.<br>2. Navigate to `/help` and back. | The box is empty. There is no draft anywhere. | — |
| POST-07 | P2 | player | The box grows to fit what is typed and the counter counts down ([The simple case](../message-board/post-and-reply.md#the-simple-case)). | Signed in. | 1. Type four lines of text.<br>2. Watch the box and the counter. | The box grows taller. The counter falls from 500. No other visible change on the first keystroke. | — |
| POST-08 | P3 | player | The counter turns yellow with fewer than 50 characters left ([While editing](../message-board/post-and-reply.md#while-editing)). | Signed in. | 1. Type 460 characters.<br>2. Read the counter's colour. | Yellow at "40 left". | — |
| POST-09 | P1 | player | Past 500 the counter turns red, shows a negative number, and **the send button goes dead** — the only rule the composer enforces as the user types ([While editing](../message-board/post-and-reply.md#while-editing)). | Signed in. | 1. Paste 520 characters into the box.<br>2. Read the counter and try to press send. | Red, "-20 left", and the button cannot be pressed. No toast is needed because the button gets there first. | — |
| POST-10 | P2 | player | Pressing Send on an empty or spaces-only box shows a red toast and sends nothing ([While editing](../message-board/post-and-reply.md#while-editing)). | Network panel open. | 1. Press Send with the box empty.<br>2. Type three spaces and press Send.<br>3. Watch the network. | "Empty message — Please enter a message before sending" both times. No request either time. | — |
| POST-11 | P2 | player | Both composer rules are **toasts**, not messages under the box, which is unlike the rest of the app's forms ([While editing](../message-board/post-and-reply.md#while-editing)). | As POST-10. | 1. Trigger the empty-message rule.<br>2. Look under and around the box. | Nothing under the box. The message is a toast that dismisses itself. | — |
| POST-12 | P1 | player | Enter inside the box adds a newline rather than sending, and line breaks are kept exactly as typed ([While editing](../message-board/post-and-reply.md#while-editing)). | Signed in. | 1. Type a word, press Enter, type another word.<br>2. Press the send button.<br>3. Read the posted message. | Enter never sends. The posted message has the same two lines. | — |
| POST-13 | P2 | slow | The box and the button are disabled while the request is in flight ([Submit](../message-board/post-and-reply.md#submit)). | Throttled connection. | 1. Type a test message and press send.<br>2. Try to type and to press send again. | Both are dead until the answer arrives, so the same message cannot be sent twice from the button. | — |
| POST-14 | P1 | player | **The username and team the browser sends are not what is stored** — the league overwrites both ([Submit](../message-board/post-and-reply.md#submit)). | A player whose display name has recently changed, or two teams to compare. | 1. Post a test message.<br>2. Read the name and team badge on the posted message.<br>3. Compare with the profile and the approved membership. | The message carries the profile's real name and the current team from the membership, whatever the browser claimed. | — |
| POST-15 | P1 | player | **A successful post produces no confirmation**: the box empties, no toast appears, and the author's own message reaches their screen through the live connection ([Submit](../message-board/post-and-reply.md#submit)). | Signed in, network panel open. | 1. Post a test message.<br>2. Count the toasts.<br>3. Watch how the message appears in the list. | The box empties, no toast, and the message arrives the same way everyone else's does. The category picker stays where it was. | — |
| POST-16 | P1 | player | **If the live connection is down, a successful post looks exactly like one that went nowhere** ([Edge cases](../message-board/post-and-reply.md#edge-cases)). **(suspected bug)** | A way to break the live connection while leaving ordinary requests working — for example a proxy that blocks the socket only. | 1. Break the live connection.<br>2. Post a test message and watch the board for 30 s.<br>3. Reload the page. | Record what happens. The expectation is an empty box and nothing else, with the message only appearing after the reload. | — |
| POST-17 | P2 | offline | On failure nothing is cleared, every word stays in the box, and one red toast appears — the same toast whatever went wrong ([Submit](../message-board/post-and-reply.md#submit)). | Composer with text, then offline. | 1. Go offline.<br>2. Press send.<br>3. Read the toast and the box. | "Error posting message — Your message could not be posted. Please try again." The text is untouched and the button is live again. | — |
| POST-18 | P2 | player | An author reaches their own message's controls by pressing it, and pressing anywhere else closes them ([Editing a message already posted](../message-board/post-and-reply.md#editing-a-message-already-posted)). | A test message of the tester's own. | 1. Click the message on a desktop.<br>2. Read the corner.<br>3. Click elsewhere on the page. | A pencil and a bin appear in the corner, then close again. Somebody else's message does nothing when clicked. | — |
| POST-19 | P2 | player | The pencil replaces the message with a focused box holding the current text, cursor at the end, and a line reading "Press Esc to cancel, Ctrl+Enter to save" ([Editing a message already posted](../message-board/post-and-reply.md#editing-a-message-already-posted)). | A test message of the tester's own. | 1. Press the pencil.<br>2. Type without clicking first.<br>3. Read the hint line. | The typing lands at the end of the existing text. That exact hint line is shown. | — |
| POST-20 | P2 | player | Save is dead until the text is both non-empty and different from what was there ([Editing a message already posted](../message-board/post-and-reply.md#editing-a-message-already-posted)). | An edit box open. | 1. Read Save before touching the text.<br>2. Delete everything and read it again.<br>3. Type one extra character. | Dead, dead, then live. | — |
| POST-21 | P2 | player | Saving an edit shows a green toast and the message comes back with "(edited)" after it ([Editing a message already posted](../message-board/post-and-reply.md#editing-a-message-already-posted)). | An edit box open with a change. | 1. Press Ctrl+Enter.<br>2. Read the toast and the message.<br>3. Hover "(edited)". | "Message updated — Your message has been updated". "(edited)" after the text, with a tooltip giving how long ago. | — |
| POST-22 | P1 | player | **The edit box has no character counter and no length limit** ([Edge cases](../message-board/post-and-reply.md#edge-cases)). **(suspected bug)** | A test message of the tester's own. | 1. Open the edit box and look for a counter.<br>2. Paste 700 characters and press Ctrl+Enter.<br>3. Read the message on the board. | Record what happens. The expectation is that a message that could not have been posted at 700 characters can be edited to 700 characters and is shown in full. | — |
| POST-23 | P2 | offline | A failed edit shows a red toast and **keeps the edit box open with the new text**, so nothing is lost ([Editing a message already posted](../message-board/post-and-reply.md#editing-a-message-already-posted)). | An edit box open with a change, then offline. | 1. Go offline.<br>2. Press Ctrl+Enter.<br>3. Read the box and the toast. | A red toast, and the edit box still open holding the new text. | — |
| POST-24 | P2 | player | The bin opens a confirmation reading "Delete Message — Are you sure you want to delete this message? This action cannot be undone." with Cancel and Delete ([Removing a message](../message-board/post-and-reply.md#removing-a-message)). | A test message of the tester's own. | 1. Press the bin.<br>2. Read the dialog. | That exact wording and those two buttons. | — |
| POST-25 | P2 | player | Delete removes the message from every filtered view at once and shows a green toast ([Removing a message](../message-board/post-and-reply.md#removing-a-message)). | A test message visible under a team filter. | 1. Apply a filter that shows the message.<br>2. Delete it.<br>3. Clear the filter. | "Message deleted — Your message has been deleted". The message is gone from the filtered list and from the unfiltered one. | — |
| POST-26 | P2 | offline | **A failed delete leaves the confirmation open with the Delete button live**, so it can be pressed repeatedly to the same effect ([Edge cases](../message-board/post-and-reply.md#edge-cases)). | A test message, then offline. | 1. Go offline.<br>2. Press the bin, then Delete.<br>3. Press Delete twice more. | "Error deleting message" each time. The confirmation stays open and nothing changes. | — |
| POST-27 | P1 | player | **A message with reactions may be undeletable** ([Open questions](../message-board/post-and-reply.md#open-questions-and-verification)). **(suspected bug)** | A test message of the tester's own, and a second account to react to it. | 1. Post a test message.<br>2. React to it from the second account.<br>3. Try to delete it from the author's account. | Record what happens. The expectation is that the delete is refused with the generic failure toast, for a message the author can then never remove. | — |
| POST-28 | P1 | player | **A player waiting for team approval may not be able to post at all**, and the toast never says why ([Open questions](../message-board/post-and-reply.md#open-questions-and-verification)). **(suspected bug)** | A fresh account that has requested to join a team and has **not** been approved. | 1. Sign in as that account and open `/message-board`.<br>2. Type a test message and press send.<br>3. Read the toast, then retry twice. | Record what happens. The expectation is the generic "could not be posted" toast, nothing about approval, and a retry that can never work. | — |
| POST-29 | P2 | player | Escape cancels an edit and throws the change away with **no confirmation**, and closes the delete confirmation without deleting ([Cancel and interrupt](../message-board/post-and-reply.md#cancel-and-interrupt)). | An edit box open with a change; then a delete confirmation open. | 1. Press Escape in the edit box.<br>2. Read the message.<br>3. Open the bin confirmation and press Escape. | The edit is discarded with no prompt. The message is unchanged. The confirmation closes and nothing is deleted. | — |
| POST-30 | P1 | player | **Everything typed is lost on navigating away or reloading, with no warning**, and a post already sent still lands ([Cancel and interrupt](../message-board/post-and-reply.md#cancel-and-interrupt)). | Throttled connection. | 1. Type a long test message and click a link away — check for any prompt.<br>2. Return and read the box.<br>3. Type another, press send, and reload within the same second. | No unsaved-changes guard either time. The half-typed message is gone. The sent one is on the board after the reload, and only the board says which happened. | — |
| POST-31 | P2 | player | A retry after a failure can post the same message twice, because nothing distinguishes "the league never got it" from "the answer was lost" ([Cancel and interrupt](../message-board/post-and-reply.md#cancel-and-interrupt)). | A failure that can be produced with the message still reaching the league, if one can be arranged. | 1. Cause a post to fail.<br>2. Press send again on the kept text.<br>3. Read the board. | Record whether the board holds one message or two. Delete any duplicate afterwards. | — |
| POST-32 | P1 | player | An expired session leaves the composer on screen; the post is then refused with the generic toast and **nothing offers to sign in again** ([Cancel and interrupt](../message-board/post-and-reply.md#cancel-and-interrupt)). | A session made to expire with the board open. | 1. Let the session expire.<br>2. Type a test message and press send.<br>3. Read the toast and the page. | The composer is still drawn. The generic red toast appears with nothing naming the session and no sign-in offer. | — |
| POST-33 | P1 | player, second browser | **An author's message deleted in another tab disappears from under an open edit box, and saving then writes nothing and still reports success** ([Cancel and interrupt](../message-board/post-and-reply.md#cancel-and-interrupt)). **(suspected bug)** | The same account signed in twice, one test message. | 1. Open the edit box in browser A.<br>2. Delete the message in browser B.<br>3. Change the text in A and save. | Record what happens. The expectation is a success toast for a write that reached nothing. | — |
| POST-34 | P2 | player, second browser | An author editing the same message in two tabs wins or loses on whoever saves last, with no warning either way ([Modifiers](../message-board/post-and-reply.md#modifiers)). | The same account in two browsers, one test message. | 1. Open the edit box in both.<br>2. Save a different change in each. | The last save wins. Neither browser is warned that the other changed it. | — |
| POST-35 | P2 | phone | On a phone the composer is fixed above the bottom navigation and the author's controls need a press and hold of about half a second ([Modifiers](../message-board/post-and-reply.md#modifiers)). | Phone, a test message of the tester's own. | 1. Scroll the board and watch the composer.<br>2. Press and hold your own message.<br>3. Press and hold somebody else's. | The composer stays pinned. Your own message opens the pencil and bin; somebody else's opens the reaction panel. The same gesture does two different things. | — |
| POST-36 | P2 | player | **Announcement is only a colour**: it notifies nobody, does not pin the message and does not keep it at the top ([Edge cases](../message-board/post-and-reply.md#edge-cases)). | Admin, and a second account watching the board. | 1. Post an Announcement.<br>2. Watch the second account's screen and check for any notification or email.<br>3. Post an ordinary message afterwards and read the order. | A blue border and a badge, nothing more. The later General message sits above the Announcement. | — |
| POST-37 | P2 | admin | The category picker survives a post ([Edge cases](../message-board/post-and-reply.md#edge-cases)). | Admin. | 1. Post an Announcement.<br>2. Read the picker.<br>3. Type again. | It is still on Announcement. The next message is an Announcement too unless it is changed back. | — |
| POST-38 | P2 | player | **Editing a message keeps its place in the list**, because the order is by when it was posted ([Edge cases](../message-board/post-and-reply.md#edge-cases)). | An old test message of the tester's own. | 1. Scroll down to an old message of your own and edit it.<br>2. Look at the top of the list. | It stays where it was, now carrying "(edited)". Nothing brings it to the top. | — |
| POST-39 | P2 | player | **The 500-character limit is only in the browser** ([Edge cases](../message-board/post-and-reply.md#edge-cases)). | The edit path from POST-22, or any way past the dead button. | 1. Get a message over 500 characters onto the board.<br>2. Read it in full. | It is stored and shown in full. Nothing in the league refuses it. | — |

Not checkable by hand:

- What happens to a message whose author's account has been removed. The document
  says it belongs to nobody and can never be edited or deleted; producing that
  state needs an account to be deleted. See
  [Modifiers](../message-board/post-and-reply.md#modifiers).
- Whether the absence of replies and of moderation is deliberate rather than
  unfinished. POST-01 and POST-02 confirm the absence; the intent is a product
  decision for the league.
- Whether the author's own message really *only* appears through the live
  connection. POST-16 needs a way to break the socket while leaving ordinary
  requests working; without one the item is `blocked`.
- Whether the author's own message card is announced usefully by a screen reader.
  The document says it is a control with no label; confirming it needs a real
  screen reader.

## help/the-help-page.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| HELP-01 | P2 | visitor | The page is headed "Help & Getting Started" with the line "Everything you need to know about using 717REC for league management and participation." ([The simple case](../help/the-help-page.md#the-simple-case)). | Signed out. | 1. Open `/help` from the main navigation.<br>2. Read the top of the page. | Both strings exactly as written. | — |
| HELP-02 | P2 | visitor | A **Quick Navigation** card holds four buttons — Teams, Schedule, Standings, Playoffs — which go straight to those pages ([The simple case](../help/the-help-page.md#the-simple-case)). | Any. | 1. Read the four buttons.<br>2. Press each and note where it lands, returning each time. | `/teams`, `/schedule`, `/stats`, `/playoffs`. Four buttons, no more. | — |
| HELP-03 | P2 | visitor | There are **nine** closed section bars, each with an icon and a title ([The simple case](../help/the-help-page.md#the-simple-case)). | Signed out. | 1. Count the bars and read every title. | Welcome to 717REC, Viewing Standings & Stats, Understanding the Schedule, Team Pages, Playoff Brackets, Message Board, League History, Accessibility & Keyboard Navigation, Frequently Asked Questions. All nine start closed. | — |
| HELP-04 | P1 | visitor | Pressing a bar opens it and **closes the one already open**; pressing an open bar closes it and leaves nothing open ([The simple case](../help/the-help-page.md#the-simple-case)). | Any. | 1. Press "Team Pages".<br>2. Press "Playoff Brackets".<br>3. Press "Playoff Brackets" again. | Only one section is ever open. Two answers can never be on screen together. | — |
| HELP-05 | P2 | admin | An admin sees an **Admin Guide** sub-heading with five further sections between Accessibility and the FAQ, and a dashboard card at the bottom ([The simple case](../help/the-help-page.md#the-simple-case)). | Signed in as admin, then as a player. | 1. Count the bars as an admin and read the sub-heading.<br>2. Count them again as a player. | Fourteen bars and a dashboard card for the admin, nine and no card for the player. The FAQ sits five sections further down for the admin. | — |
| HELP-06 | P3 | visitor | The page arrives without breadcrumbs and without the fade-in the rest of the app uses ([Arrive](../help/the-help-page.md#arrive)). | Fresh session. | 1. Navigate from `/stats` to `/help` and watch the transition.<br>2. Compare with a navigation from `/stats` to `/teams`. | `/help` appears more abruptly, and no breadcrumb trail is drawn. | — |
| HELP-07 | P2 | browser | One request goes out on arrival: the league's current power score weights ([Arrive](../help/the-help-page.md#arrive)). | Network panel open, fresh session. | 1. Open `/help`.<br>2. Read the request list. | Exactly one data request. Everything else on the page is fixed text carried in the app. | — |
| HELP-08 | P2 | admin | The weights are used in the power score bullet under "Viewing Standings & Stats" and in the first FAQ answer, and the page quotes whatever is live ([Arrive](../help/the-help-page.md#arrive)). | Admin able to change the weights in the Power Score Sandbox. | 1. Read both places and note the numbers.<br>2. Change one weight in the sandbox.<br>3. Reload `/help` after five minutes and read both again.<br>4. Put the weight back. | Both places quote the live weights and both change together. Record how long the change takes to appear. | — |
| HELP-09 | P2 | offline | With the weights request failing, the standard 40 / 45 / 15 split is used and **nothing tells the reader the numbers may not be the league's** ([Cancel and interrupt](../help/the-help-page.md#cancel-and-interrupt)). | Network blocked, and a league whose live weights are **not** 40 / 45 / 15. | 1. Block the network.<br>2. Open `/help` and open "Viewing Standings & Stats" and the FAQ.<br>3. Read the numbers and look for any caveat. | The page renders in full because its text is part of the app. The numbers read 40 / 45 / 15 with nothing marking them as defaults. | — |
| HELP-10 | P1 | visitor | **The help page cannot be searched**: closed sections are not on the page, so find-in-page matches only the nine titles ([Edge cases](../help/the-help-page.md#edge-cases)). | Any. | 1. With every section closed, use the browser's find-in-page for a word you know is inside a section body.<br>2. Open that section and search again.<br>3. Look for a search box of the app's own. | No match while closed, a match once open. There is no search box on the page. | — |
| HELP-11 | P2 | visitor | Which section was open is not remembered anywhere, and coming back gives a fully closed page ([Leave without changing anything](../help/the-help-page.md#leave-without-changing-anything)). | Any. | 1. Open a section.<br>2. Navigate to `/stats` and back to `/help`. | Every section closed again. Nothing was stored in the browser. | — |
| HELP-12 | P1 | visitor | **No answer has an address.** The URL never changes, so a particular answer cannot be linked to, bookmarked, or sent to somebody who asked the question ([Edge cases](../help/the-help-page.md#edge-cases)). | Any. | 1. Open a section and read the address bar.<br>2. Copy the URL, open it in a fresh tab. | `/help` with no fragment and no query string. The fresh tab opens fully closed. | — |
| HELP-13 | P1 | visitor | **The Contact page link reloads the whole app** rather than navigating in place ([Edge cases](../help/the-help-page.md#edge-cases)). **(suspected bug)** | Network panel open, fresh session. | 1. Open the Accessibility & Keyboard Navigation section.<br>2. Find the Contact page link at its foot.<br>3. Press it and watch the network panel and the page. | Record what happens. The expectation is a full page load — the whole app fetched again — instead of an instant in-app navigation. | — |
| HELP-14 | P2 | visitor | **The contact form is described as reachable from the footer, but the footer holds only an email address** ([Edge cases](../help/the-help-page.md#edge-cases)). | Any page. | 1. Read the footer on `/help` and on `/`.<br>2. Look for a link to `/contact`. | An email address, not a link to the form. The routes to `/contact` are the main navigation and this page's Accessibility section. This contradicts [`contact-the-league.md`](../help/contact-the-league.md#summary). | — |
| HELP-15 | P1 | visitor | **The Team Pages section describes tabs the team page does not have** ([Edge cases](../help/the-help-page.md#edge-cases)). **(suspected bug)** | Any. | 1. Open "Team Pages" and read every name it gives.<br>2. Open a real team page and look for those names. | Record both lists. The expectation is that the help page names a Stats Tab, a Matches Tab, an H2H Tab and an Achievements Tab, while the team page is built from collapsible sections with those names rather than tabs. | — |
| HELP-16 | P2 | visitor | The FAQ's answer about playoff seeds hedges where the rest of the page states facts ([Edge cases](../help/the-help-page.md#edge-cases)). | Any. | 1. Open the FAQ and read the playoff seeds answer. | It reads "typically based on regular season standings or power rankings". It is the one answer that does not commit. | — |
| HELP-17 | P2 | visitor | The Accessibility section makes claims about the product that nothing in the app enforces ([Edge cases](../help/the-help-page.md#edge-cases)). | Any. | 1. Open the Accessibility section and read the list of screen readers and the touch-target promise.<br>2. Compare with the badge and bracket findings in [`standings.md`](standings.md) (BADGE-24, BRACKET-24). | The section promises 44-pixel touch targets and names screen readers. Record the exact wording; several surfaces in the app do not meet it. | — |
| HELP-18 | P3 | phone | The Quick Navigation grid drops to two columns on a phone and everything else stays one column ([Modifiers](../help/the-help-page.md#modifiers)). | Phone, or 390 × 844. | 1. Open `/help` and read the grid.<br>2. Open the Accessibility section and read the keyboard-shortcut table. | Two columns. The shortcut table stays a two-column list, which is the tightest thing on the page. | — |
| HELP-19 | P2 | browser | Tab reaches the four Quick Navigation links and every section bar; Enter and Space open a section; arrow keys move between the bars ([Modifiers](../help/the-help-page.md#modifiers)). | Fresh load. | 1. Tab through the page and note every stop.<br>2. Press Enter on one bar and Space on another.<br>3. Press Down and Up on a focused bar. | That order. Both keys open a section. The arrows move the focus between bars, as a proper accordion. | — |
| HELP-20 | P2 | browser | **Escape does nothing**, and there is no key that closes an open section other than pressing its bar again ([Modifiers](../help/the-help-page.md#modifiers)). | A section open. | 1. Press Escape.<br>2. Try Backspace and Delete on the focused bar. | The section stays open. Only its own bar closes it. | — |
| HELP-21 | P1 | browser | **Opening a section adds nothing to history, so Back from an open section leaves the page entirely** ([Cancel and interrupt](../help/the-help-page.md#cancel-and-interrupt)). | Arrived at `/help` from `/stats`, then a section opened. | 1. Open a section.<br>2. Press browser back once.<br>3. Read where you are. | Record what happens. The expectation is a return to `/stats` rather than the section closing. | — |
| HELP-22 | P2 | browser | A reload closes the open section and loses nothing else ([Cancel and interrupt](../help/the-help-page.md#cancel-and-interrupt)). | A section open. | 1. Reload. | The same fully closed page. Nothing else on the page has state to lose. | — |
| HELP-23 | P2 | admin, second browser | An admin changing the weights does not reach an open page; the reader keeps the old numbers until something causes a refetch ([Cancel and interrupt](../help/the-help-page.md#cancel-and-interrupt)). | Browser A on `/help` with the power score bullet open, browser B an admin. | 1. Change a weight in browser B.<br>2. Watch browser A untouched for six minutes.<br>3. Leave browser A's tab and return.<br>4. Put the weight back. | Nothing changes while watching. Returning to the tab can quietly refresh the weights, so the sentence can change between one look and the next. | — |
| HELP-24 | P2 | admin | An expiring session makes the admin sections vanish when the app notices ([Cancel and interrupt](../help/the-help-page.md#cancel-and-interrupt)). | Admin on `/help` with an Admin Guide section open. | 1. Let the session expire, or sign out from a browser sharing it.<br>2. Watch the page and then reload. | The five extra sections and the dashboard card go. An already-open section can be pushed up or down the page as they do. | — |
| HELP-25 | P2 | visitor | The page publishes its four FAQ answers in a form search engines read, with the league's live power score weights inside them ([Interactions with other systems](../help/the-help-page.md#interactions-with-other-systems)). | Any. | 1. View the page source and find the machine-readable FAQ block.<br>2. Read the weights inside it. | Four questions and answers, carrying the same weights the visible copy shows. | — |

Not checkable by hand:

- Whether the help content should be editable by an admin. The README's plan lists
  "help content" as an admin surface and no such surface exists at this commit;
  whether the plan or the product is wrong is a decision, not a test. See
  [Open questions](../help/the-help-page.md#open-questions-and-verification).
- How the accordion behaves with a screen reader, and whether opening a section
  moves focus or announces the new content. Needs a real screen reader.
- What the machine-readable FAQ summary looks like in a search result, and whether
  the weights inside it were live when the page was crawled. HELP-25 checks the
  markup; the search result is outside the app.
- Whether the Accessibility section's differing appearance — no icon panel, no
  border — is deliberate or an oversight. A design question for the league.
- Whether the help page and the admin dashboard's Getting Started tab should be
  one document. Both exist and neither points at the other; merging them is a
  product decision.
