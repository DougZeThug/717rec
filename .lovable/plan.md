

## Plan: Bump 5 dependencies to latest minor/patch versions

### What's changing

These are the Dependabot-flagged updates. Four are safe minor/patch bumps; one (`uuid` 13 → 14) is a major bump that needs a quick check.

| Package | From | To | Risk |
|---|---|---|---|
| `@capgo/capacitor-social-login` | 8.3.14 | 8.3.16 | Patch — safe |
| `@supabase/supabase-js` | 2.103.3 | 2.104.0 | Minor — safe |
| `@tanstack/react-query` | 5.99.0 | 5.99.2 | Patch — safe |
| `react-hook-form` | 7.72.1 | 7.73.1 | Minor — safe |
| `uuid` | 13.0.0 | 14.0.0 | **Major** — verify usage |

### `uuid` v14 check

v14's main breaking change is dropping Node 18 support and tightening ESM exports. Project usage is already the modern `import { v4 } from 'uuid'` pattern (mocked in `tests/__mocks__/supabase.ts`), which v14 keeps unchanged. No code edits expected — but if the build surfaces an import resolution error, fall back to `import { v4 as uuidv4 } from 'uuid'` per v14's named-export guidance.

### Steps

1. Update the five version pins in `package.json`.
2. Run `npm install` (uses existing `legacy-peer-deps=true` from `.npmrc`).
3. Run `npx tsc --noEmit` and `npm run build` to confirm a clean compile.
4. If `uuid` causes any import error, adjust the affected import sites (search confirms only test mocks reference it directly).
5. Spot-check the auth flow (Supabase client) and one form (react-hook-form) in preview to confirm runtime behavior.

### Files touched

- Edit: `package.json` — five version bumps.
- Edit: `package-lock.json` — regenerated by `npm install`.
- Possibly edit: any `uuid` import site if v14's export shape needs adjusting (unlikely).

### Rollback

Revert `package.json` + `package-lock.json` and re-run `npm install`. One step.

