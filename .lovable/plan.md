
## Plan: Bump 4 dependencies (Dependabot production-dependencies group)

### What's changing

All four are safe minor/patch bumps within the same major versions.

| Package | From | To | Risk |
|---|---|---|---|
| `@capgo/capacitor-social-login` | 8.3.14 | 8.3.17 | Patch — safe |
| `react-router` | 7.14.1 | 7.14.2 | Patch — safe |
| `react-router-dom` | 7.14.1 | 7.14.2 | Patch — safe |
| `@vitest/coverage-v8` | 4.1.4 | 4.1.5 | Patch — safe (dev dep) |

Note: the Dependabot group lists "7 updates" but only 4 packages were provided. Proceeding with the 4 listed.

### Steps

1. Update the four version pins in `package.json` (uses existing `legacy-peer-deps=true` from `.npmrc`).
2. Run `npx tsc --noEmit` to confirm a clean TypeScript build.
3. Run `npm run build` to confirm the Vite production build succeeds.
4. Spot-check routing in preview (any `<Link>` / route navigation) to confirm `react-router` runtime behavior is unchanged.

### Files touched

- Edit: `package.json` — four version bumps.
- Edit: `bun.lock` — regenerated by the install step.

No source code changes expected — all bumps are patch-level within the same major.

### Rollback

Revert `package.json` + `bun.lock` and reinstall. One step.
