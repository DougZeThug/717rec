# Bump dependencies

Update `package.json` to the requested versions. Lockfile (`package-lock.json`) will be regenerated by the install step.

## Version bumps

**Production dependencies**
- `@capgo/capacitor-social-login`: 8.3.17 → 8.3.20
- `@sentry/react`: 10.49.0 → 10.51.0
- `@supabase/supabase-js`: 2.104.0 → 2.105.1
- `@tanstack/react-query`: 5.99.2 → 5.100.9
- `lucide-react`: 1.8.0 → 1.14.0
- `nanoid`: 5.1.9 → 5.1.11
- `react-hook-form`: 7.73.1 → 7.75.0
- `react-resizable-panels`: 4.10.0 → 4.11.0
- `zod`: 4.3.6 → 4.4.2

**Dev dependencies**
- `globals`: 17.5.0 → 17.6.0
- `jsdom`: 29.0.2 → 29.1.1
- `lovable-tagger`: 1.1.13 → 1.3.0
- `postcss`: 8.5.10 → 8.5.13

## Steps

1. Verify each package's current version in `package.json` (and confirm whether it lives in `dependencies` vs `devDependencies`).
2. Edit `package.json` to set the new versions, preserving existing range prefixes (`^`, `~`, or pinned).
3. Reinstall so `package-lock.json` updates to match. `.npmrc` already enforces `legacy-peer-deps=true`.

## Risk notes

All bumps are minor or patch releases — no major version changes — so breaking changes are unlikely. The only one worth a quick spot-check after install:

- **`lucide-react` 1.8 → 1.14**: largest jump in this set. Will confirm icon imports still resolve (project uses many `lucide-react` icons via `src/icons/iconRegistry.ts`).
- **`@tanstack/react-query` 5.99 → 5.100**: same major (5.x), no API changes expected; existing query/cache patterns unaffected.
- **`zod` 4.3 → 4.4**: same major, additive changes only.

The harness will run typecheck/build automatically after the install, which will surface any incompatibility immediately.

## Files touched

- `package.json` (13 version strings)
- `package-lock.json` (regenerated by install)

No source code, no test, no config changes beyond `package.json`.
