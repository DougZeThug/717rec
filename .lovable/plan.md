

## Dev Dependency Updates

Update 2 dev dependencies to their latest versions.

### Changes to Apply

| Package | From | To | Notes |
|---------|------|-----|-------|
| @types/node | 25.1.0 | 25.2.0 | Minor version update |
| eslint-plugin-react-refresh | 0.4.26 | 0.5.0 | Minor version update |

### Implementation

Update `package.json` devDependencies section with the new version numbers for both packages.

**Note**: The eslint-plugin-react-refresh update from 0.4.x to 0.5.0 is technically a minor version bump following semver for 0.x versions (where minor versions can include breaking changes). However, this plugin is stable and the update should be straightforward.

