/**
 * The line a division card shows when it holds no brackets.
 *
 * Shared because two components can render it: the card itself, and
 * `DivisionProjectedSeeds` when it has no seeds to show instead.
 */
export const NO_BRACKETS_COPY = 'No brackets yet for this division';
